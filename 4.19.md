# 4月19号进展汇报

## 总体进度

**交付件**：

- 高质量SFT数据：**✅ 第一批已打包交付**（556条pass query，超过500目标）
- 图增强检索：**本周重点，新高 R@10 = 0.6913**
- 1040-doc生产线：**正在跑 enrich pipeline**（job 61649）



## 一、检索实验（Track A）

**本周核心结论：图增强检索效果已确认，但 R@10 上限瓶颈已现**

### 新高

- **R@10 = 0.6913**（explicit bridge edges + static_plus_neighbor rerank，Qwen3-Embedding-0.6B）
- **R@1 = 0.2505**（0.6B + v2_chunk 图作 rerank context，static_prior）——超过 4B 基线（0.2421）

### 本周关键发现

| 实验 | 结论 |
|------|------|
| v2_chunks 作 candidate pool | 有害（R@1 −7.2pp）；作 rerank graph 有效（R@1 +1.2pp vs 4B） |
| Cross-doc section 边（bug fix 后） | R@10 小幅提升，但被 explicit-only 覆盖 |
| Typed cross-doc 元素边（figure/formula/table） | v1_enriched corpus 下 R@10 +1.5pp；v2chunk corpus 下反而有害 |
| Summary 虚拟节点作检索信号 | **无直接 R@1/MRR 提升**，但 summary 节点保留——作跨文档结构 scaffold 和 embedding 用途 |
| 大规模 sweep（17种配置） | 全部并列 R@10=0.6913，与 explicit-only 基线持平 |

### 现状判断

> R@10 = 0.6913 可能是当前图结构的天花板。R@100 = 0.8636，说明 dense 召回没问题，瓶颈在 top-10 reranker 精度，不在 corpus 边。

### to-do

- ~~paragraph merge 结果待补~~：**✅ 已补（见 Section 五）**，结论：chunk corpus 不改变 baseline 位置
- entity/keywords 虚拟节点：**暂缓**，等新的图结构思路（sweep 证明直接作检索边无效；embedding 用途另议）
- QA 轴验证：**blocked**，等 C-Pool qrels 标注方案



## 二、数据生产（Track B）

### 已完成

- **第一批打包交付** ✅（556条，53 docs，cross-modal 100%）
- Pass query 库：L3 × 4配置 + M2 × 2配置 sweep，通过率约 26-47%

### 库存明细

| 来源 | 类型 | Pass |
|------|------|------|
| sweep_2026-04-12（6配置） | L3 + M2 | 369 |
| l3_enriched_v3 + new82 | L3（旧hub） | 146 |
| m2_diverse + long_chain | M2 + 长链 | 41 |
| **合计** | | **556** |

### to-do

- neg evidence 标注方案：**未开始**，需设计（4.16 遗留）
- LLM QC 调用次数简化：已将 ablation 改为单次批量，grounding 单次，**共 2次/query**（4.16 to-do 已完成）
- 简短自然问题（C-Pool 78条）：ranking 已跑，**无 qrels**，等 mentor 决定标注路线



## 三、1040-doc 生产线（Track C）

### 现状

- 1040 docs 元素提取 ✅，但 LLM enrich 仅 6707/27209（24.6%）——之前 job 因并发全部 403
- **Job 61649 正在跑**（36b，顺序执行，有 coverage gate）：
  - Step 1：hub_shortchain 759 elements enrich
  - Step 2：gap227 4301 elements enrich
  - Step 3-7：merge + pair 生成（1156 top-25% pairs + 467 gap227 pairs）→ `hub_candidates_v2_combined.json`
  - 预计完成：~30-40 min

### to-do

- 61649 完成后：基于 combined pairs 跑 1040-doc 首批 query 生成
- 验证 1040-doc query 质量（与旧 53-doc 集对比 pass rate）



## 四、专利 / 论文

- **4.25 专利申请**：图结构检索方法已有实验支撑（R@10 0.6913 vs BM25 0.9xx R@10 but lower MRR）
- **5.25 ARR deadline**：需要 QA 轴实验 + 更大规模数据（1040-doc）
- to-do：图文档（技术文档1/2/3）已完成，专利文本准备中



## 五、Paragraph Merge 完整结论（job 33 step3，4.20 补充）

朋友在集群跑完了 33_paragraph_merge_step3，结果如下：

v1_enriched（基线）：R@1=0.2389，R@10=0.5994，MRR=0.6081  
v3_n400（400词合并）：R@1=0.1691，R@10=0.4937，MRR=0.4794  
v3_n500（500词合并）：R@1=0.1723，R@10=0.5032，MRR=0.4854  

n500 略优于 n400（R@1 +0.003），但两者均远低于 v1_enriched。**Claim C_PARA 否定**：paragraph merge 无法超越 element-level enriched baseline。

**性能下降的真正原因——corpus 稀释，不是 chunk 本身的问题**：

三份 qrels 内容完全相同（946 条，2.000 正例/query），指向的都是 246 个 unique element passage。但 corpus 规模不同，gold passage 在候选池中的密度从 8.8%（v1_enriched: 246/2809）降到 5.2%（v3_n400: 246/4772），额外多出的 4526 个 chunk passages 全部是非 gold 的"噪声"，让模型更难在 top-1 命中正确答案。

chunk 语义上包含了 bridge paragraph 的内容，但因为 qrels 不记录 chunk ID，命中后也得不到 credit——这是评估设计的问题，不是 chunk retrieval 本身的问题（见 Section 六）。



## 六、评估设计缺陷分析（4.20 补充）

### 6.1 Query 生成 vs 评估的错位

所有 query 由 element pair 生成，推理路径包含桥接段落，例如：

```
path: [figure_4, 1709.02012::p::00010, table_2]
qrels:   ✅ figure_4            ❌ 无               ✅ table_2
```

Bridge paragraph 节点（`::p::` 格式，来自 LaTeX ref graph）：
- 用于 query 生成，让 query 有推理深度
- 从未出现在任何 corpus 或 qrels 中
- 格式与 corpus 的 `{doc_id}_chunk_{i}` 完全不同，无法直接映射

### 6.2 Soft Positive 扩展失效

`build_graph_augmented_corpus.py` 里的 `extend_qrels()` 设计通过 section 名称将同 section 的 chunk 加为 soft positive，但实际添加数量为 **0**——因为 element 的 `section` 字段在多数记录里是空字符串，`sections_overlap("", "Introduction")` 返回 False，导致扩展逻辑完全没有生效。

### 6.3 结论与修正方向

当前评估测的是：在 element-level corpus 里同时召回 query 两端的两个 element，不考虑桥接段落。这对 v1_enriched（全 element）是公平的，对 chunk corpus 是不公平的（chunk 包含 bridge 内容但不得分）。

公平评估 chunk retrieval 需要的方案（按难度排序）：

**方案 A（短期可行）**：建立 `element_id → parent_chunk_id` 映射表，将 qrels 里的 element passage_id 同时映射到其所在 chunk，让 chunk 命中也能得分。

**方案 B（彻底正确）**：将 `::p::` bridge node 也导出为 corpus passage，并加入 qrels，将每条 query 的 gold standard 从 2 个 element 扩展到完整推理路径（element_a + bridge_para + element_b）。

短期不动 qrels，结论维持：v1_enriched 作为 element-level retrieval baseline 是准确可信的，chunk corpus 实验的低分数字不具有直接可比性。



## 当前阻塞

| 阻塞项 | 依赖 |
|--------|------|
| QA 轴实验 | C-Pool qrels 标注方案（mentor决定） |
| paragraph merge GPU eval | 手动重跑（参数 bug 已知） |
| 1040-doc query 生成 | job 61649 完成（~30-40min） |
