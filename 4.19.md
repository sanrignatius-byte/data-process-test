# 4月19-21号进展汇报

## 上周任务对齐

| 任务 | 状态 |
|------|------|
| 交付第一批 SFT 数据（500 条） | ✅ 556 条 pass query |
| 图增强检索实验（重新验证） | ✅ 做了，发现旧实验有问题，已修正口径 |
| 1040-doc 生产线 | ✅ 已自动化，hub pair candidates 产出 |

---

## 一、图现在怎么建的

### chunk 怎么加进来

之前 chunk 只连了 paragraph 和 section，**没有连 element**。导致 chunk 进入检索库之后跟我们的 evidence（figure/table/formula）是断开的，也就是说图结构根本传不到 chunk 上。

修正方案：

- chunk 里显式注入区域内 element 的描述文本（caption + enriched_content）
- 补 `chunk → element` 的 contains 边（用 LaTeX 行号对齐，不用模糊匹配）
- qrels 从 `element_id` 重映射到 `parent_chunk_id`，让 chunk 命中能得分

这样 chunk 才算真正是图上的节点，explicit 投影边才能在 chunk 空间起作用。

### enrich 现在做到哪里

| 节点 | 状态 |
|------|------|
| Element（hub pair 路径上的） | 部分完成，6707/27209（旧 813-doc 子集） |
| Element（gap227 文档） | ✅ 完成，4283/4301 enriched（99.6%） |
| Section | 旧 53 docs 完成 |
| Chunk | 没有单独 enrich，语义来自注入的 element 内容 |

**合计**：production_full 共 10988/27209 elements enriched（40.4%），1040 docs 全有覆盖。hub_candidates_v2_combined 里 75% 的 gap227 pairs 两端都已 enriched。

**注意**：chunk 要先等 element enrich 完再建，不然注入的内容是空的。

---

## 二、检索实验做了什么

### 背景：为什么要重做这个实验

Mentor 要证明"图到底帮不帮得到检索"。之前我们在 element 空间上做的对比图是有效的（`explicit_only + static_plus_neighbor` 在 473 query 上 R@10=0.6913，是本地 SOTA），但 Mentor 质疑的是 **chunk 作检索单元**这条线——当前工业界的 RAG 主流口径是 chunk，如果图只能在 element 粒度上起作用，这个故事是不完整的。所以这轮重点是验证：**把 chunk 正式作为图节点 + 把 element 语义注入 chunk + 用图结构做 rerank**，能不能让 chunk-level retrieval 达到甚至超过 element-level。

### 旧实验为什么不算

之前把 element corpus 和 chunk corpus 混在一起做检索（4673 条），但 qrels 只认 element_id，chunk 被命中也不得分。这个实验不能回答"chunk 适不适合作为检索单元"，口径有两个错：

1. **Corpus 稀释**：element passage + chunk passage 混在同一索引里，gold element 在 corpus 中的密度被冲淡，检索器更容易挑中相邻的 chunk 但拿 0 分
2. **Qrels 失配**：qrels 只有 `element_id`，chunk 命中拿不到任何 credit——等于在强迫检索器在两种节点类型里只能命中 element 才算数

另外还有一个隐蔽问题：早期 paragraph-merge 版 chunk 把 element 注入放在 passage **末尾**，`max_length=512` 直接把 element 语义切掉。v2 (element-first + max_length=1024) 证明 truncation 是大问题，但还不足以说明 chunk 已经优于 element。

### 这次怎么做的

拆成三条独立的 lane，每条 lane corpus、qrels、图结构都单独对齐。共同前置修正：

- **Chunk 里显式注入 element 语义**：区域内每个 figure/table/formula 的 caption + enriched_content 直接拼进 chunk passage，不再靠"chunk 恰好包含一段引用"
- **补 `chunk_contains_element` 边**：用 LaTeX 行号对齐定位（不用模糊 `context_before` 匹配），chunk↔element 在图上真正连通
- **Qrels 重映射**：`element_id → parent_chunk_id`，chunk 命中按其包含的 gold element 计分
- **Element-first 注入 + max_length=1024**：避免 truncation 把 element 语义切掉

Chunk 图结构至少包含四类边：`chunk_contains_paragraph` / `chunk_contains_element`（新增）/ `section_contains_chunk` / `chunk_sequence`。Rerank 脚本 `scripts/eval_chunk_graph_rerank.py` 显式区分 `chunk_sequence`（序关系）和 `explicit_projected`（element-pair 投影到 chunk 空间的桥边）。

| Lane | 描述 | Corpus / 图结构 | 状态 |
|------|------|----------------|------|
| graph-only fair | chunk 只含 raw paragraph + element visible text，**无 enrich** | n400: 1963 passages，n500: 1703 passages，qrels 覆盖 460/473 queries | ✅ 跑完（Job 62476/62477） |
| partial-overlay | 叠加旧的部分 enrich overlay（813 docs / 6707 enriched elements） | 同上 corpus 拓扑 + enriched 注入 | ✅ 跑完（Job 62553/62554） |
| fair enriched | 补全 trial57 backfill 后的全量 enrich 公平比较 | — | ❌ 阻塞（company API `az.gptplus5.com` 持续 401，Job 62482 backfill 0 enriched，62495 coverage guard 47.2%<95% 失败） |

### 结果

**Element baseline 参考**（作为天花板）：`v1_enriched` 在 473 queries 上 R@1=0.2389 / R@5=0.5127 / R@10=0.5994 / MRR=0.6081。

**Lane 1：graph-only fair（纯文本 chunk，无 enrich）**

| Variant | R@1 | R@10 | MRR |
|---------|-----|------|-----|
| n400 dense | 0.0902 | 0.3348 | 0.2587 |
| n500 dense | 0.0967 | 0.3609 | 0.2855 |
| n400 BM25 | 0.0837 | 0.3359 | 0.2574 |
| n500 BM25 | 0.0826 | 0.3652 | 0.2696 |

- Dense 和 BM25 基本打平 → 纯文本 chunk 没有语义优势
- R@10 只有 0.33–0.36，相对 element baseline 0.5994 腰斩 → 纯文本 chunk 严重弱于 element
- n500 始终略优于 n400（约 +0.02–0.03）

**Lane 2：partial-overlay dense**

| Variant | R@1 | R@10 | MRR |
|---------|-----|------|-----|
| n400 dense | 0.1500 | 0.5109 | 0.3975 |
| n500 dense | 0.1587 | 0.5304 | 0.4196 |
| n400 BM25 | 0.1924 | 0.5522 | 0.4831 |
| n500 BM25 | 0.2054 | 0.5870 | 0.5054 |

- 注入 enrich 后 R@10 从 0.33 跳到 0.51–0.59 → enrich 对 chunk 语义密度的提升是关键驱动
- **BM25 > Dense**（R@10: 0.59 vs 0.53）：enriched 内容强化了关键词匹配面，dense encoder 在这个粒度上反而吃亏
- 但 R@1/MRR 仍低于 element baseline，chunk-only 还没赢

**Lane 2 + 图 rerank（n400）**

`eval_chunk_graph_rerank.py` 用两种信号 × 两种 prior 做 2×2 ablation：

| Config | R@1 | R@10 | MRR |
|--------|-----|------|-----|
| dense_baseline | 0.1500 | 0.5109 | 0.3975 |
| seq_only / static_prior | 0.1489 | 0.5011 | 0.3966 |
| seq_only / static+neighbor | 0.1250 | 0.4511 | 0.3474 |
| exp_only / static_prior | 0.1935 | 0.6533 | 0.4871 |
| exp_only / static+neighbor | 0.2011 | 0.6489 | 0.4992 |
| seq+exp / static_prior | 0.2109 | **0.6598** | 0.5168 |
| seq+exp / static+neighbor | **0.2261** | 0.6391 | **0.5297** |

- `chunk_sequence` 单独用是负作用（相邻 chunk 被错误当成亲近），印证了 C2（intra-doc 虚拟边稀释 precision）在 chunk 粒度上同样成立
- `explicit_projected`（element 桥边 → chunk 空间投影）是有效信号
- `seq+exp / static_prior` 的 R@10=0.6598 **超过 element baseline 0.5994**，方向上走通了
- 但最佳 R@1=0.2261 仍略低于 element baseline 0.2389，**还没在 top-1 精度上真正翻盘**

### 结论

分两层说（前后不矛盾）：

1. **Fair 结论（chunk-only 没 enrich）**：chunk-only 明显不如 element，R@10 腰斩。纯文本切分拿来做检索单元是退步
2. **方向结论（partial overlay + graph rerank）**：chunk 注入 enrich 内容 + explicit 投影边 rerank，R@10 已经能超 element baseline。方向是对的
3. **核心评估还差一块**：**fair enriched lane（全量 enrich 下的公平比较）还没跑通**，company API 一直 401，不能下定论说"chunk 路线已经全面赢了 element"
4. **精 top-1 仍是 element 的主场**：R@1 在任何配置下 chunk 都没超过 element 的 0.2389。这提示 chunk 更合适的定位可能是"检索后消费单元 / QA 单元"，而不是"替换 element 作主检索节点"

关联的 claim：这轮结果主要支持 C1（explicit static prior 有效），再次验证 C2（intra-doc 虚拟边稀释 precision，chunk_sequence 同构问题）；C7 的"explicit_only + static+neighbor R@10 高"在 chunk 空间部分成立（`exp_only` R@10=0.6489 已接近），但 R@1 弱势说明在 chunk 粒度上需要补新的 top-1 信号。

详细实验记录见 `research-wiki/experiments/20260421_chunk_as_retrieval_unit.md` 和 `research-wiki/experiments/20260421_trial57_fairness_repair.md`。

---

## 三、数据生产

跟建图没关系，老方法，直接堆量。

- 已交付 556 条，53 docs，100% cross-modal
- QC 简化：2 次调用 / query（去幻觉 + 全 evidence 依赖检查）
- 图的作用是 shortlist + pair 选择，不替代标注

---

## 四、下一步

**最优先（等 API 通了）**：补跑 trial57 backfill enrich，跑 fair enriched lane，给出公平口径下的最终结论

**当前主线**：element-level 检索仍是最稳的，chunk 路线作为"检索后消费单元 / QA 单元"来推进，不作为主检索的竞争方案

**暂缓**：summary 直接检索、entity/keyword 虚拟节点、长链路线
