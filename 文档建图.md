# 文档建图

## 一、建图目标

当前建图支撑三件事：

1. **检索提升**：用图结构帮助 dense retrieval 把排名靠后但真正相关的证据提上来；
2. **数据合成**：为 query / evidence / answer 生产提供结构化的候选池；
3. **QA 组织**：把检索到的证据整理成适合下游大模型消费的单元。

建图原则：**先建准单文档内的元素关系，再考虑跨文档和虚拟节点。不把图做复杂。**

---

## 二、数据来源

建图用到两种原始解析结果，都来自同一批 PDF：

| 来源 | 解析器 | 产出 |
|------|--------|------|
| PDF → 结构化元素 | MinerU | 文本段落（text element）、图（figure）、表（table）、公式（formula）、章节层级（section） |
| LaTeX 源码 | 自研 `\label`/`\ref` 解析 | 精确引用边、共引关系、行号定位 |

两者通过 LaTeX 行号对齐。MinerU 解析结果对应到 LaTeX 源码的具体行，从而可以精确匹配元素之间的引用关系。

---

## 三、节点设计

### 3.1 统一概念：所有基本单元都是"元素"

建图的基本单元统一称为**元素（element）**，共四种：

| 元素类型 | 英文 | 来源 | 说明 |
|----------|------|------|------|
| 文本元素 | text element | MinerU 解析得到的段落（paragraph） | 论文正文中的一段话 |
| 图元素 | figure element | MinerU 解析得到的图 | 含 caption、图片路径 |
| 表元素 | table element | MinerU 解析得到的表 | 含 caption、表格内容 |
| 公式元素 | formula element | MinerU 解析得到的公式 | display math，不含 inline math |

四种元素在图中是**平级关系**，都是 MinerU 从 PDF 中解析出来的基本单元。

**inline 公式不算独立元素**。只有 `\begin{equation}` / `\begin{align}` / `$$...$$` 这类独立成行的公式才算 formula element，行内的 `$...$` 视为文本元素的一部分。

### 3.2 章节元素

章节元素（section / subsection / subsubsection）来自 MinerU 解析的文档层级结构。

作用：
- 提供文档结构定位（某个元素属于哪个章节）
- 为检索提供结构先验（同一章节的元素相关性更高）
- 章节本身**不作为检索单元**，只作为图中的结构辅助节点

### 3.3 切片（chunk）

切片是本项目的**检索消费单元**。它的定义是：

> 将同属一个最底层 section 的连续文本元素，按固定长度（n400 ≈ 400 词，n500 ≈ 500 词）合并。遇到 major section 边界强制断开。

构建方式：
1. 以 MinerU 解析出的文本元素（段落）为最小合并单元；
2. 使用 NLTK / spaCy 做英文分词，按词数切分；
3. section 边界处强制断开，避免跨章节合并；
4. 切片文本拼接所包含图/表/公式元素的 caption 和 LLM 描述文本（enriched_content），使切片的文本表示包含多模态语义。

**切片不是虚拟节点**，它是检索与下游消费的实际工作单元。

**切片与元素的关系**：
- 一个切片可以包含多个文本元素和多个图/表/公式元素；
- 切片通过 LaTeX 行号确定它包含哪些元素（`chunk_contains_element` 边）；
- 切片替代元素成为检索命中单元，但元素仍然是 query 生成和证据标注的基本单位。

---

## 四、边设计

### 4.1 核心边的分类

图的边分为三类：**结构边**（文档层级）、**引用边**（`\ref` 关系）、**切片边**（chunk 层的等价边）。

### 4.2 结构边

用 LaTeX 行号排序确定。反映了论文的自然阅读顺序。

| 边 | 含义 | 怎么建的 |
|----|------|----------|
| 阅读顺序边 | 一个文本元素后面跟着另一个文本元素 | 按 LaTeX 行号排序，相邻即连 |
| 章节包含元素 | section 包含某个文本/图/表/公式元素 | 元素的行号落在 section 的行号范围内 |

### 4.3 引用边

解析 LaTeX 源码中的 `\label{...}` 和 `\ref{...}` 得到。

| 边 | 含义 | 怎么建的 |
|----|------|----------|
| 元素引用边 | 文本中 "see Figure 3" 引用了图3 | 解析 `\ref{fig:xxx}`，匹配到对应 `\label{fig:xxx}` |
| 元素到元素引用 | 图/表/公式之间互相引用 | 解析 `\ref` 目标为另一个 element label 的情况 |

引用边的价值：它代表了**作者明确认为存在逻辑关联的元素对**，这类边的可靠性远高于基于内容相似度推断的边。

### 4.4 切片边

切片是合并文本元素得到的，因此需要在新层级重建边。

| 边 | 含义 | 怎么建的 |
|----|------|----------|
| 切片包含元素 | 切片在构建时包含了哪些元素 | 通过 LaTeX 行号精确匹配（元素的行号落在切片的行号范围内） |
| 章节包含切片 | 切片属于哪个 section | 切片的段落都来自该 section |
| 切片相邻 | 两个切片在文档中前后相邻 | 按切片顺序 |

### 4.5 投影边

投影边（projected edge）是将元素层的引用边映射到切片层。

**规则**：如果元素 A 和元素 B 在元素层有引用边，且元素 A 落在切片 X 中、元素 B 落在切片 Y 中，则给切片 X 和切片 Y 添加一条投影边。

**作用**：把元素层已证明有效的图结构信号搬到切片层，增强切片层检索的图 rerank 能力。

**实验验证**（2026-04-21）：投影边在切片空间是有效信号。纯切片阅读顺序边对 rerank 有负作用（把分数扩散到了无关邻居），但投影边 + 阅读顺序边组合使用时，切片层的 R@10 超过了元素层 baseline。

---

## 五、图增强检索（graph rerank）

### 5.1 整体流程

```
dense retrieval（BGE-M3 / Qwen3-Embedding-4B）
    → top-100 候选切片
        → 图 rerank（对 top-100 内的切片做结构性加分，重新排序）
            → top-10 最终结果
```

图 rerank 不做召回扩展（不引入 top-100 之外的新候选），只是在现有候选内重新排序。

### 5.2 三项加权信号

对 top-100 中的每个切片，根据图结构施加三种加分：

| 加权项 | 含义 | 计算方式 |
|--------|------|----------|
| 出入度加权 | 在图中连接越多的节点越重要 | 计算该切片在切片图中的归一化度数（连接的邻居数）作为静态先验分 |
| 阅读顺序分数传播 | 排名高的切片给相邻切片加分 | 从排名高处向排名低处，按阅读顺序边做一跳传播。排名高的邻居给排名低的邻居加分 |
| 引用边分数传播 | 通过引用/投影边关联的切片互相加分 | 如果切片 X 排名高，且 X 与 Y 有引用边或投影边，则 X 给 Y 加分 |

三项加权汇总为一个最终分数，重新排序得到 top-10。

**传播约束**：只允许排名高的给排名低的加分（从上往下传播），不允许反向。每条边的传播加分有上限，防止一个节点被大量邻居加到不合理的位置。

### 5.3 枢纽节点

枢纽（hub）是从图中选出的高价值节点，按以下规则打分：

```
hub 分数 = 0.40 × 桥接得分（同时连接不同模态元素的节点）
         + 0.35 × 连接度（与多少其他节点相连）
         + 0.25 × 核心章节（是否在方法/实验等核心章节）
         + 0.10 × 关键词加权
         + 20 × PageRank
```

取 top 10%-20% 作为 hub。Hub 主要用于：
- 确定哪些元素需要 LLM 做多模态描述（enrich），因为 hub 周边的元素是 query 生成的高频候选；
- 在图 rerank 中，如果候选切片属于 hub 的物理邻域（位置落在 hub 的附近），获得额外加分。

---

## 六、元素增强（enrich）

### 6.1 什么是 enrich

对图/表/公式元素，用大模型生成一段自然语言描述（100-200 词），描述该元素的内容、关键信息、与上下文的关联。这段描述文本之后会被注入到切片中，使切片在 dense embedding 空间中有多模态语义。

### 6.2 不全量做

由于 API 调用成本和时间限制，enrich 不全量做。当前 enrich 覆盖：

| 覆盖范围 | 状态 | 说明 |
|----------|------|------|
| 枢纽周边的元素（hub pair 路径上） | 约 25% | 6707/27209 个元素，已完成 |
| 长尾元素（gap227 文档） | 0% | API 401 阻断 |
| 全量元素 | 0% | 成本过高，暂不计划 |

### 6.3 构建顺序

```
Step 1: 元素 LLM enrich（对确定要 enrich 的元素做多模态描述）
Step 2: build_paragraph_chunks.py → 合并文本元素为切片，注入元素的 caption + enriched_content
Step 3: 评测
```

**注意**：切片的 "enrich" 不是独立做的，而是通过所包含元素的 `enriched_content` 注入实现。如果元素未被 enrich，切片就只有 paragraph 原文 + caption，这被称为 "fair" 状态。

---

## 七、检索公平性约束

所有检索实验必须满足：

1. **检索单元统一**：比 element 就全部用 element corpus，比 chunk 就全部用 chunk corpus，不混用；
2. **标注对齐**：如果切片是检索单元，qrels 要从 `element_id` 重映射到 `chunk_id`，否则命中的切片不算得分；
3. **enrich 口径透明**：必须明确报告 enrich 覆盖了多少元素、哪些模态、是否存在偏置；
4. **负样本清洁**：当前用 top-11 ~ top-15（embedding 相似度排序）做 hard negative，存在混入假负样本的风险，后续需加 LLM 过滤。

---

## 八、chunk 的后续用途

切片（chunk）在项目中的定位：

1. **检索消费单元**：dense retrieval + graph rerank 的输出是切片列表；
2. **QA 证据单元**：检索到的切片直接喂给大模型做问答，比整 section 省 token；
3. **query 生成证据单元**：chunk（含注入的 element 语义）作为 query 生成脚本的 evidence 输入。

chunk → query 生成的标准路径：

```
hub pair candidates → 定位端点元素 → 定位元素所在切片 → 生成 query（evidence = chunk）
```

---

## 九、虚拟节点与 summary 节点（暂缓）

以下方向属于辅助层级，**不作为当前主线**：

- **summary 节点**：对 section 做语义浓缩，作为结构辅助节点保留。但作为直接检索信号目前没有稳定正收益，暂不纳入主 claim。
- **跨文档引用边**：保留为后续跨文档方向的候选，当前单文档建图优先。
- **语义聚类虚拟节点**：对语义相近的切片做聚类生成虚拟节点，属于探索性方向，暂缓。

---

## 十、检索公平性约束（旧实验对照）

### 10.1 元素 → 切片 对齐

旧实验的 qrels 是基于 element_id 标注的。用切片做检索单元时，需要把 `element_id` 映射到 `parent_chunk_id`，否则评测无效。

**方案 A（推荐）**：切片作为唯一检索单元
```
corpus = 切片 passages only
qrels = element_id → parent_chunk_id（重映射）
```

**方案 B（兼容旧实验）**：元素 + 切片联合认可
```
corpus = 元素 passages（主）
qrels 同时认可 element_id 和 parent_chunk_id
```
缺点：评测口径不干净。

当前建议：新实验统一走方案 A。

---

## 十一、当前主结论（2026-05-10 更新）

### 已验证

- 元素层图 rerank 在 57 篇 gold doc 上有稳定正收益，R@10 从 dense baseline 0.6195 提升到 0.6913（+7.2pp）
- 图 rerank 的增益集中在图元素和表元素上（figure +10.3pp, table +8.3pp），公式元素无增益（formula +0pp）
- 公式检索 R@10 ≈ 0.56 的瓶颈是 dense encoder 而非图拓扑 — 6 个独立配置全部 ≤ 0.56

### 待验证

- 用 math-aware encoder（Qwen3-Math / Mistral-Math）替换公式元素的 embedding，看能否突破 0.56 天花板
- 全量 enrich后的 fair enriched chunk 是否优于 element baseline（blocked by API 401）
