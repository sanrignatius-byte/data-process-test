# 文档建图



## 一、文档建图

- pdf -> mineru -> multimodal_elements_graph
  - 提取 figure/table/formula/section，建文档内 DAG
  
- latex -> latex_reference_graph
  - 解析 `\label` 跟 `\ref`，建精确引用边
  
- （跨文档）latex-> citation_graph
  - `.bbl` 标题模糊匹配，建跨文档边

后面需要建一个**总图**，将之前的三个图合并成一张图



### 1.1 总图：建节点

- 段落（1种）：
  - paragraph：LaTeX 空行分隔的段落块，文本格式

- 章节标题（1种）：
  - section / subsection / subsubsection，文本格式，来源LaTeX

- 多模态元素（3种）：
  - figure / table：mineru抠图
  - equation：仅含 `\begin{equation}` 带 label 的独立公式，忽略in-line equation
  - 这一步，会进行latex跟mineru的交叉验证，匹配不上的就丢掉了，大概50%



### 1.2 总图：建边

| 边类型 | 来源 | 说明 |
|--------|------|------|
| `backbone` | 行号排序 | 自然阅读顺序 |
| `paragraph_ref` （==含权重==） | LaTeX `\ref` | 段落→元素，权重 = log₂(1 + 引用次数)  |
| `element_ref`                  | LaTeX `\ref` | 元素→元素，同上下文被共引             |
| `section_contains_paragraph` | 行号范围 | 最内层 section 包含段落 |
| `section_contains_element` | 行号范围 | 最内层 section 包含元素 |
| `cross_doc_cite` | ~~.bbl 匹配~~ | ~~源论文 top-2 段落→目标论文 top-2 元素~~ |

- 过于复杂，三种边就够了，==需要简化==
  - 自然关系：阅读顺序
  - 引用关系：不同element的 `\ref` 关系
  - 结构关系：hierarchy层级关系



### 1.3 Hub节点选择

**Hub节点选择**：

所有的节点都会去根据规则打分，top 10-20%（==需要确定==）的节点称为hub节点。

如下的节点比较大概率成为hub：

- 段落节点
- 引用多模态元素的节点
- 包含关键词、page-rank分高的节点

**打分规则**：

```
hub_score = 0.40 × bridge + 0.35 × connectivity + 0.25 × core_module
          + 0.10 × keyword_boost + 20 × pagerank − penalty
```

- **bridge**：出边连 ≥2 种模态 → 100，1 种 → 50，0 → 0
- **connectivity**：min(1, (总度数 + 跨类型边数) / degree_norm) × 100
- **core_module**：正则匹配 section 名（introduction=1.0, method=0.8, related work=0.3 等），段落继承所属 section 的分
- **keyword_boost**：全局规则（introduction/architecture/summary/structure）× 100
- **penalty**：入度 > 出度×2 且只连 ≤1 种模态 → 扣 20 分（压制 authority sink）



## 二、节点enrich

如何针对目前的总图进行enrich？

- 实体/关键词增强
  - 每个element（段落or多模信息）进行实体/关键词抽取，建立虚拟节点
  - 利用实体/关键词虚拟节点，进行虚拟边连接。比如利用多个overlap的关键词，联系两个element
- summary或者描述增强
  - 每个section生成一段summary
  - 多模态element生成描述
- embedding增强
  - 利用不同element之间的embedding相似度，进行虚拟边连接



---

下面是当前的实现，需要大改

### 2.1 Hub节点枚举（==考虑删掉==）

有些hub节点连接了太多的元素，目前实现方案只支持hub+两个元素/段落的情况，如果hub节点有超过两个元素，则继续枚举，产生多个hub节点的简化variants。

四种策略从 bridge hub 出发：

| 策略       | 路径结构                                                    | 跳数  |
| ---------- | ----------------------------------------------------------- | ----- |
| Strategy 1 | [elem_A, hub, elem_B]（hub 同时引用两种模态）               | 2-hop |
| Strategy 2 | [elem_A, hub, backbone邻居, elem_B]（邻居段落引用另一模态） | 3-hop |
| Strategy 3 | [elem_A, hub, elem_B_他文]（跨文档 citation）               | 2-hop |
| Strategy 4 | [elem_A, section, elem_B]（section 包含 ≥2 模态）           | 2-hop |



### 2.2 Enrich Hub节点

enrich范围：针对hub节点进行enrich，用于query生成

- 利用大模型，对多模态元素进行描述，对段落信息进行总结
- hub所有信息根据空格进行分词，屏蔽非重要词汇，然后把top10的词作为关键词


