# 文档建图

## 一、建图目标

当前建图的目标不是“把图做复杂”，而是支撑三件事：

1. **检索提升**：帮助 dense retrieval 更早找到真正证据；
2. **数据合成**：为 query / evidence / answer 生产提供稳定的结构化 shortlist；
3. **QA 组织**：把检索到的证据整理成更适合下游大模型消费的单元。

因此，图中不同节点的职责需要明确分开：

- **element / paragraph graph**：负责细粒度证据和桥接关系；
- **chunk**：负责检索消费单元；
- **虚拟节点 / 虚拟边**：只作为辅助层，不作为当前主卖点。

---

## 二、总图组成

### 2.1 基础图来源

- `pdf → mineru → multimodal_elements_graph`
  - 提取 `figure / table / formula / section`
  - 建文档内多模态结构

- `latex → latex_reference_graph`
  - 解析 `\label / \ref`
  - 建精确引用边与共引关系

- `latex → citation_graph`
  - 基于 `.bbl` 模糊匹配建立跨文档 citation 边

最终目标是合成一张总图，但在当前阶段，**检索验证优先使用文档内可解释、可对齐的边**。

### 2.2 图的规模与版本

**全局规模（v2，`data/01_graphs/latex_graph_topology_report_v2.json`）**：

| 指标 | 值 |
|------|---|
| 节点总数 | 247,457 |
| 边总数 | 390,598 |
| paragraph | 182,670 |
| figure | 14,795 |
| table | 10,459 |
| equation | 3,251 |
| cross_doc_cite | 0（v2 暂关，见 §4.1） |

评测时主线使用的是从总图切出的子集（如 `gold57` 等），不是全图；具体子集规模见各自评测报告。

**版本演进（仅列对语义有影响的变化）**：

| 版本 | 关键改动 | 失败模式 |
|------|---------|---------|
| v1 | 首版拓扑 | `page_idx` 全为 0（MinerU parser bug），无法做页级定位 |
| v2 | 用 `content_list.json` 的 `position_idx` 重建页索引；新增 `adjacent_backbone_bridges` 检测；默认关闭 cross-doc 边 | 当前主评测口径 |
| v3（cross-modal 精修） | Jaccard 阈值 + 数字后缀 fallback，label 匹配率 28.8% → 49.8% | 仅用于 cross-modal pair 产出 |

旧评测数字（v1）与新评测数字（v2）不能直接比对，版本在汇报时必须声明。

---

## 三、节点设计

### 3.1 Paragraph 节点

- 来源：LaTeX 空行分隔段落
- 作用：
  - 保留原始细粒度文本证据
  - 作为 element 的天然桥接文本
  - 作为后续 chunk merge 的基础单元

### 3.2 Section 节点

- 类型：`section / subsection / subsubsection`
- 作用：
  - 提供层级定位
  - 提供结构先验
  - 不作为当前主检索单元

### 3.3 多模态 Element 节点

- 类型：
  - `figure`
  - `table`
  - `equation`

- 作用：
  - 代表 query / evidence 中最关键的多模态原子证据
  - 是当前 element-level retrieval 和 graph rerank 的核心节点

### 3.4 Chunk 节点（检索消费单元）

Chunk 的定位需要明确：

- **Chunk 不是虚拟节点**
- **Chunk 是检索与下游消费的工作单元**

构建原则：

- 基于相邻 paragraph 合并（`build_paragraph_chunks.py`）
- 目标长度：n400 / n500（约 400–500 词）；major section boundary 处强制切块
- **必须在 element enrich 完成后再建 chunk**，否则 enriched_content 字段为空

**Chunk passage 内容**

```text
chunk text = 段落文本
           + [Figure/Table/Formula]: caption + enriched_content
```

- element 不再作为独立文本条目构成平行语料；
- chunk 中必须显式包含区域内 element 语义，否则 multimodal 信息在 dense retrieval 中天然吃亏；
- 图像/表格/公式的 `enriched_content` 必须在 element enrich 完成后才可注入。

**对齐方式：LaTeX 行号优先**

| 方式 | 可靠性 | 推荐 |
|------|--------|------|
| LaTeX 行号 / 段落定位字段 | 高，唯一稳定 | ✅ 优先 |
| `context_before` 模糊字符串匹配 | 低，易漂移 | 仅 fallback |

`chunk_contains_element` 边必须基于 LaTeX 行号建立，不能用字符串模糊匹配替代。

**Chunk 的职责定位**

- 已落地：作为检索单元参与 dense retrieval + graph rerank（qrels 用 element_id → parent_chunk_id 重映射，见 §9.3 方案 A/B）；
- 已落地：作为下游 QA / agent 的证据消费单元；
- **未落地**：作为 query 生成时 evidence 的最小完整单元。当前 query 生成仍在 element 层（见 §9.1），chunk 尚未进入 `generate_multihop_l1_queries.py` 的 evidence 装配路径。

---

## 四、边设计

### 4.1 基础结构边

| 边类型 | 来源 | 说明 |
|--------|------|------|
| `backbone` | 行号排序 | 自然阅读顺序 |
| `paragraph_ref` | LaTeX `\ref` | 段落 → 元素 |
| `element_ref` | LaTeX `\ref` / 共引 | 元素 → 元素 |
| `section_contains_paragraph` | 行号范围 | section 包含 paragraph |
| `section_contains_element` | 行号范围 | section 包含 element |
| `cross_doc_cite` | `citation_graph` 标题模糊匹配 | **v2 暂关**（0 条）；保留接口，待跨文档评测重启后再开 |

关于 `cross_doc_cite`：v1 曾启用 434 条跨文档引用边，v2 关闭原因是当前 gold 评测集以文档内为主，跨文档边会引入 recall 分散（hub 评分被跨文档高被引节点拉偏）。接入 element-level cross-doc edges（见 CLAUDE.md）后再考虑重开。

### 4.2 Chunk 结构边

| 边类型 | 来源 | 说明 |
|--------|------|------|
| `chunk_contains_paragraph` | chunk merge | chunk → paragraph |
| `chunk_contains_element` | **LaTeX 行号**（优先）/ 窗口对齐（fallback） | chunk → element |
| `section_contains_chunk` | 层级范围 | section → chunk |
| `chunk_sequence` | chunk 顺序 | 相邻 chunk 互连 |

关键约束：

- `chunk_contains_element` 是 chunk 图成立的核心边；没有这条边，chunk 只是文本窗口，无法承接 explicit graph 投影；
- `chunk_sequence` 单独使用时对 rerank 有负作用（顺序传播把分数扩散到错误邻居），必须与 `explicit_projected` 组合使用；
- 后续所有 chunk-space rerank，必须依赖 `chunk_contains_element` 做 `element → chunk` 映射。

### 4.3 投影边：`explicit_projected`

`explicit_projected` 不是第一类边，而是从 element 层图派生到 chunk 空间的**投影边**，定义如下：

```text
对 hub pair candidate 路径 (element_a, ..., element_b)：
  chunk_a = chunk_contains_element^{-1}(element_a)
  chunk_b = chunk_contains_element^{-1}(element_b)
  → 在 chunk 图中加边 (chunk_a, chunk_b)，权重 = hub pair 质量分
```

作用：把 element 层的多跳桥接信号（hub pair 多模态桥）映射为 chunk 图中的跨段语义边，补 `chunk_sequence` 的漂移问题。

实现脚本：`scripts/eval_chunk_graph_rerank.py`（用 `--projected-edges` 参数开启）。没有 `chunk_contains_element` 边时投影为空 → 退化为 seq-only。

**实验验证结果（2026-04-21）**

| Edge config | R@10 | 相对基线 |
|------------|-----:|------:|
| dense baseline（partial overlay） | 0.5109 | — |
| seq_only | 0.4511 | -0.060 |
| exp_only | 0.6533 | +0.142 |
| seq+exp（static_prior） | **0.6598** | **+0.149** |

`explicit_projected` 投影边在 chunk 空间是有效信号，超过 element-level baseline（0.5994）。

---

## 五、图结构分工

### 5.1 实体层 graph 的价值

实体层 graph（paragraph / element / section）的价值，主要体现在：

1. **桥接证据**
   - 例如 paragraph 同时引用 figure 和 table
   - 这是 query 生成和 evidence 组织的重要中间层

2. **图 rerank**
   - dense 检索先给出候选
   - graph 再对真正相关但排名靠后的证据做结构性提升

3. **数据生产 shortlist**
   - 用 hub / pair 机制先筛，再 enrich，再生成 query

因此，实体 graph 的作用不是替代 dense retrieval，而是：

```text
dense retrieval 负责召回
entity graph 负责结构校正和证据桥接
```

**当前实际部署的 rerank 算子**（`scripts/eval_graph_topk_rerank.py` / `eval_chunk_graph_rerank.py`）：

| 算子 | 信号 | 关键参数 | 备注 |
|------|------|---------|------|
| `graph_static_prior` | 节点 degree 先验 | `w_static × log(1 + degree)` | 低成本、全局稳定 |
| `graph_neighbor_prop` | 1-hop 邻域分数传播 | `neighbor_decay = 0.20` | 稳健，两轮评测一致 +0.0039 Recall |
| `graph_hub_rerank` | Hub 邻域加权 | `hub_weight (hw)` | 对 hub coverage 敏感 |
| `graph_citation_walk` | 跨文档引用游走 | `citation_decay = 0.30` | 当前为负贡献，graph_full 中默认关闭 |
| `static_plus_neighbor` | 上两者线性合并 | 两权重独立调 | 当前 chunk 评测的 static_prior 主配置 |

组合策略由 `--hub-weight / --nprop-weight / --cite-weight` 解耦控制；Phase0 v3 最优配置为 `hw=0.15, nw=1.00, cw=0`。

### 5.2 Chunk 的价值

Chunk 的价值不应再表述成“图更丰富”，而应表述成：

1. **更适合作为检索消费单元**
   - 比单 paragraph 更完整
   - 比整 section 更短、更省 token

2. **更适合作为下游 QA 单元**
   - 被检索到后，可以直接送入 reader / agent

3. **承载 element 注入后的多模态语义**
   - 把图像 / 表格 / 公式的文字语义带入 dense embedding 空间

但当前实验结果也说明：

- chunk-only dense retrieval **尚未优于** element-level baseline；
- 因此 chunk 目前更合理的定位是：
  - 检索消费单元
  - QA 消费单元
  - rerank 空间

而不是当前 retrieval 主战场。

---

## 六、Hub 与 Enrich

### 6.1 Hub 节点选择

所有节点根据规则打分，选 top 10%-20% 作为 hub。

高价值 hub 一般包括：

- 引用多模态元素的 paragraph
- 连接多种模态的 bridge 节点
- 核心 section 内高连接度节点

**打分规则**

```text
hub_score = 0.40 × bridge
          + 0.35 × connectivity
          + 0.25 × core_module
          + 0.10 × keyword_boost
          + 20 × pagerank
          - penalty
```

**两类 hub 并存**（`scripts/analyze_latex_graph_topology.py`）：

| 类别 | 定义 | 当前数量 | 用途 |
|------|------|---------|------|
| `bridge_hubs` | 同时引用 ≥2 种模态的 paragraph | v2: 2,548 | 多跳 candidate pair 的首选 seed |
| `adjacent_backbone_bridges` | backbone 上相邻两段 paragraph 分别引用不同模态 | v2: 397 | 补足 bridge_hubs 覆盖不到的"隐式桥"文档 |

两者合并后参与 hub coverage 计算。只用 `bridge_hubs` 时，hub coverage 从 90.42% 降至 9.53%，neighbor_prop 信号会显著失效——这是 Phase0 v2 → v3 的关键修复。

### 6.2 Enrich 设计

当前 enrich **不全量做**，原因是成本与吞吐限制。但"为了支撑公平对比，enrich 必须扩量"是当前阻塞项，不是已经达成的现状。

**Enrich 覆盖分层（必须在汇报中明确）**

| 节点类型 | Enrich 状态 | 说明 |
|----------|------------|------|
| Element（hub pair 路径上） | ⚠️ 部分 | 6707/27209，仅 hub 子集；且集中在历史 76 docs |
| Element（gap227 文档） | ❌ 未完成 | job 61526 输出文件未落盘，等同未补齐，需要重跑 |
| Element（评测 gold 集全量） | ❌ 未达到 95% | 公平评测所需阈值，当前未满足 |
| Section | ⚠️ 仅旧 53 docs | section-level enrich 未覆盖新增文档 |
| Chunk | ❌ 无单独 enrich | 见下条说明 |

**关于 chunk 的 enrich 口径（重要修正）**：

- chunk 目前**没有独立的 enrich 流程**，也没有被注入到任何已在运行的 query 生成 / 检索评测路径中；
- 之前文档里"chunk 语义通过 element enriched_content 注入实现"描述的是**规划路径**，不是当前已落地的状态；
- 只要 query 生成仍在 element 层（见 §9.1），chunk 侧是否注入 enriched_content 都不会影响当前 qrels；
- 因此 **"chunk enrich = element enrich 注入" 不能作为公平对比的结论基础**，需要等到 chunk 真正接入 query 生成 + qrels 重映射之后再谈。

**为了支撑公平对比必须补齐的 enrich 工作**（当前未完成）：

1. 重跑 gap227 的 element enrich（修复 API 并发问题，产出 `multimodal_elements_v2_gap227_enriched.json`）；
2. 合并得到评测 gold 集上 ≥95% 的 element enrich 覆盖；
3. 如果走 chunk 路线，再做 chunk build + element enriched_content 注入 + `chunk_contains_element` 边；
4. 以上三步任意一步未完成，"enriched fair" 结论都不能报。

**构建执行顺序（必须严格遵守）**

```text
Step 1: element LLM enrich（hub pair endpoints + gap docs）
        ↓
Step 2: build_paragraph_chunks.py
        → 注入 element enriched_content（[Figure]/[Table]/[Formula]）
        → 建 chunk_contains_element 边（LaTeX 行号对齐）
        ↓
Step 3: （可选）chunk-level summary
        → 仅为 QA packaging 备用，不作为主检索信号
        ↓
Step 4: 评测（R@1 / R@2 / R@5 / R@10 / R@100 / MRR）
```

**为什么 R@2 重要**：部分 query 的 evidence 来自两个不同 element（cross-modal bridge），单 R@1 无法反映双证据命中率；R@2 专门对应这类双证据任务。

**检索公平性要求**（不可绕过）：

1. enrich coverage < 95% gold qrel 中可 enrich 元素 → 不能报 enriched fair 结论；
2. `build_chunk_corpus.py` 的 `--min-enriched-coverage` guard 是正式防线，不得绕过；
3. 所有汇报必须披露：enrich 覆盖率 / passage 构造方式 / 是否 partial 偏置。

---

## 七、虚拟节点 / 虚拟边的处理原则

### 7.1 当前结论

虚拟边不是当前主线，应降级处理。

原因：

- summary 作为直接 retrieval 信号目前没有稳定正收益；
- 本地顺序型虚拟边可能稀释前排精度；
- entity / keyword 虚拟节点还没有证明比基础图更值钱。

### 7.2 当前建议

- `summary`：保留为 scaffold / 结构辅助层，不作为 retrieval 主 claim
- `entity / keyword`：暂缓，等主检索验证稳定后再加
- `typed cross-doc edges`：保留为跨文档方向的候选，但不抢当前主叙事

也就是说，当前汇报中的虚拟边定位应为：

```text
备选增强层，而不是主卖点
```

---

## 八、检索公平性约束

当前所有检索实验必须满足以下公平性要求。

### 8.1 检索单元统一

- 比 element，就全部使用 element corpus；
- 比 chunk，就全部使用 chunk corpus；
- 不再用 `element + chunk` 混合库直接判断谁更优。

### 8.2 qrels 对齐统一

如果 chunk 作为检索单元，则：

```text
qrels：element_id → parent_chunk_id
```

否则 chunk 命中不得分，评测无效。

### 8.3 enrich 口径透明

必须明确：

- enrich 覆盖哪些模态、哪些节点；
- 是否只 enrich hub；
- 是否存在 partial enrich 带来的检索偏置。

### 8.4 负样本 clean

当前 `top11-top15` 的 soft negative 主要基于 embedding 相似度，风险是：

- 可能选到假负样本；
- 可能污染训练或评价。

因此后续需要补：

- clean negative 筛选规则；
- 或人工抽查 / LLM 过滤策略。

---

## 九、chunk 的下游通道

### 9.1 chunk → query 生成

**当前实际状态（2026-04-22 核查）**：chunk **尚未**进入 query 生成路径。`generate_multihop_l1_queries.py` 读入的 candidate 文件（如 `latex_cross_modal_pairs.json` / `hub_candidates_enriched_v*.json`）只含 `element_a_id` / `element_b_id`；`_context()` 从 element 的 `context_before / context_after / enriched_content` 取 evidence；输出 JSONL 的 qrels 字段是 `element_ids` + `path`，**没有 `chunk_id`**。

也就是说：

- 当前 query 的 evidence 单元 = element（及其周围 context），不是 chunk；
- 当前 qrels 主键 = element_id，不是 chunk_id；
- "chunk 参与 query 生成"是**规划路径**，尚未实现。

**规划路径（目标状态，未落地）**：

```text
hub pair candidates（2-3 hop，top 25%）
        ↓
endpoint elements（必须先完成 LLM enrich）
        ↓
chunk 定位（chunk_contains_element，LaTeX 行号对齐）
        ↓
query 生成脚本
  evidence = chunk（而非单 element）
  query text 引用 element 的 [Figure/Table/Formula] 描述
        ↓
产出 qrels：query_id → chunk_id（主键）
          + element_id（辅助，用于反查图路径）
```

要落地这条规划路径，至少需要：

1. 改 `generate_multihop_l1_queries.py`，把 evidence 来源从 element context 换成 chunk 文本（或 chunk+element 拼接）；
2. 在生成阶段写入 `chunk_id` 到输出 JSONL；
3. 对存量 queries 做 `element_id → chunk_id` 回填，或重新生成；
4. 评测代码 qrels 主键切换到 `chunk_id`。

以上任何一步未完成前，"dense retrieval 直接命中 chunk 且无需重映射"这一结论不成立。

### 9.2 chunk → 检索训练

```text
chunk corpus（n500 推荐，consistently 优于 n400）
        ↓
dense retrieval（BAAI/bge-m3）
        ↓
graph rerank（explicit_projected + seq 组合，static_prior 优先）
        ↓
top-k chunk 候选
        ↓
hard negative mining
  来源：top-11 ~ top-15（embedding 相似度排序）
  风险：可能混入假负样本，需 LLM 过滤或人工抽查
        ↓
SFT triplet：(query, positive_chunk, hard_neg_chunk)
        ↓
检索模型微调
```

**当前状态**：这条通道的 dense + rerank 部分已有实验支撑（partial overlay lane），但 SFT triplet 生产和模型微调尚未启动。

### 9.3 评估方案 A / B

**方案 A（推荐）**：chunk 作为唯一检索单元

```text
corpus = chunk passages only
qrels = element_id → parent_chunk_id（重映射）
指标：R@1 / R@2 / R@5 / R@10 / R@100 / MRR
```

- 优点：口径清晰，可以直接对接下游 QA
- 条件：必须补 `chunk_contains_element` 边和 qrels 重映射

**方案 B（兼容旧实验）**：element + chunk 联合认可

```text
corpus = element passages（主）
qrels 同时认可 element_id 和 parent_chunk_id
```

- 优点：可以复用旧 qrels，不需要全量重映射
- 缺点：评测口径不干净，不能直接支撑 "chunk 优于 element" 的 claim

**当前建议**：新实验统一走方案 A；旧实验的对比数字用方案 B 描述，不混用。

---

## 十、当前推荐的主汇报口径（2026-04-21 更新）

分三层结论汇报：

**Layer 1（Fair 结论，可直接汇报）**

- element-level explicit graph rerank 在 57 gold docs 上有稳定正收益（C1 支持）
- chunk-only dense（无 enrich）弱于 element baseline（R@10: 0.3348 vs 0.5994）
- chunk_sequence 单独使用有负作用；explicit_projected 是有效信号

**Layer 2（方向性正结论，需标注 non-fair）**

- partial overlay + explicit rerank 的 chunk 路线 R@10=0.6598，已超过 element baseline 0.5994
- BM25 在 partial overlay 下强于 dense，提示 enriched text 质量明显提升
- n500 consistently 优于 n400

**Layer 3（待验证，需 API key 修复）**

- fair enriched chunk 是否真正优于 element：blocked by company API 401
- fair 口径下图 rerank 是否有效：等 trial57 backfill 完成后可立即补跑

当前图结构的核心卖点应表述为：

```text
用细粒度实体图组织多模态证据，
chunk 注入 element 语义后作为检索与 QA 消费单元，
explicit 投影边在 chunk 空间已经显示出超过 element baseline 的 rerank 能力，
等 API 修复后，fair enriched 结论可以立即补全。
```
