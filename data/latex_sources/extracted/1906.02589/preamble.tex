% Recommended, but optional, packages for figures and better typesetting:
\usepackage{microtype}
\usepackage{graphicx}
% \usepackage{subfigure}
\usepackage{subcaption}  % Elliot: hopefully this doesn't break anything...
\usepackage{booktabs} % for professional tables
\usepackage{multirow, boldline}
% EXTRAS
\usepackage{tikz}
\usetikzlibrary{shapes.misc, positioning}

% For links in bibtex which are too long and cut across columns
\usepackage{url}
\def\UrlBreaks{\do\/\do-}
\usepackage{breakurl}
\usepackage[breaklinks]{hyperref}

% hyperref makes hyperlinks in the resulting PDF.
% If your build breaks (sometimes temporarily if a hyperlink spans a page)
% please comment out the following usepackage line and replace
% \usepackage{icml2019} with \usepackage[nohyperref]{icml2019} above.
\usepackage{hyperref}

% Attempt to make hyperref and algorithmic work together better:
\newcommand{\theHalgorithm}{\arabic{algorithm}}

% Use the following line for the initial blind version submitted for review:
% \usepackage{icml2019}

% If accepted, instead use the following line for the camera-ready submission:
\usepackage[accepted]{icml2019}

% The \icmltitle you define below is probably too long as a header.
% Therefore, a short form for the running title is supplied here:
\icmltitlerunning{Flexibly Fair Representation Learning by Disentanglement}



%%%%%%%% EXTRA PACKAGES %%%%%%%%%%%%%%%%%
\usepackage{amssymb}
\usepackage{amsmath}
%\usepackage{hyperref}
\usepackage{natbib}
\usepackage{xcolor}
% \usepackage{subcaption}
% \PassOptionsToPackage{caption=false}{subfig}
% \usepackage{subfig}

%%%%%%%% MACROS %%%%%%%%%%%%%%%%%
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\argmax}{\operatorname{argmax}}

% SHORTHAND FOR COMMENTS
\newif\ifcomments
% \commentsfalse
\commentstrue
\ifcomments
  \newcommand{\colornote}[3]{{\color{#1}\bf{#2: #3}\normalfont}}
\else
  \newcommand{\colornote}[3]{}
\fi
\newcommand {\EC}[1]{\colornote{blue}{EC}{#1}}
\newcommand {\DM}[1]{\colornote{green}{DM}{#1}}
\newcommand {\JJ}[1]{\colornote{red}{JJ}{#1}}
\newcommand {\MW}[1]{\colornote{red}{MW}{#1}}
% FEEL FREE TO MAKE YOUR OWN MACRO BELOW

