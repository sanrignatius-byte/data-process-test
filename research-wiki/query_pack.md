# Research Wiki Query Pack

_Auto-generated by `/research-wiki query "next step"` on 2026-05-20._

## Project Direction

Build M4-style multimodal scientific-document data and graph artifacts from PDF/MinerU-first parsing, with typed element nodes, intra-doc hard references, cross-doc bridges, multi-hop chains, and downstream query/session generation. Current strategic pivot: stop relying on LaTeX-only structure; make MinerU graphs work for non-LaTeX PDFs.

## Top Gaps

- `gap:G10` **PDF-first cross-doc multimodal edges**: MinerU + CLIP + caption/context rerank gives cross-doc recall but not strong edges. On 3238 xdoc visual edges, 87.2% have `caption_sim=0`, only 5.1% have real text support, and 35.8% of captions are unusable after parsing. This is now the binding research gap.
- `gap:G8` **Element-level cross-doc bridge layer**: M4 chains need element-to-element cross-doc edges, not only citation/doc-level links. May 19 design exists; May 20 edge audit says the current xdoc visual layer should stay recall-only until stronger rerank is added.
- `gap:G9` **Chain-to-session projection**: multi-turn sessions should project from validated L3 chains rather than be generated from scratch. Useful, but depends on having reliable cross-doc chain edges first.
- `gap:G3` **Deliverable SFT packaging**: 556 pass queries exist; latest/best clean chunk delivery was pushed as gzip on 2026-05-18. Graph pairing was not exact and should not be mislabeled as official.
- `gap:G7` **Modality-style mismatch**: corpus-side visual/NL injection and generic rerankers often regress because queries are paper-domain text-style while visual/formula enrichments are domain-detached.

## Recent Session Facts

- 2026-05-18 Codex: latest/best query delivery pushed: `data/03_queries/M4query_v2_clean_chunk_aug` with `corpus.jsonl.gz` and `train_triplets.jsonl`. Exact paired graph was not found; do not publish related graphs as official companion artifacts without derivation.
- 2026-05-19 Codex: built MinerU topology/VL/bridge/hub scripts and ran real `open_clip` over 53 docs. Initial quality audit found CLIP xdoc useful for recall but risky as hard edges.
- 2026-05-19 Claude/Wiki: pivoted to PDF/MinerU-first. Raw `structure.json` alone is text-sparse, so markdown/content-list fallback became necessary before broad API/query generation.
- 2026-05-20 Claude/Wiki: completed the MinerU CLIP cross-doc pipeline, switched formula similarity to `math-similarity/Bert-MLM_arXiv-MP-class_arXiv`, locked baseline, and wrote C15-C17 plus G10/idea008.
- 2026-05-20 Codex: constructed `exp:20260520_idea008_phase0_judge_pack`, a 160-pair judge-ready sample from 3003 deduped xdoc visual candidates. Output: `data/05_eval/idea008_phase0_judge_pack_latest`; status is constructed, not judged.
- 2026-05-20 Codex: ran `exp:20260520_idea008_text_only_self_judge`, a text/context-only probe over 32 balanced candidates. Result: 7 strong edges, 10 weak-related, 15 hard rejects/insufficient. Feasible candidate pool, but VLM judge still required.

## Supported Claims To Carry Forward

- `claim:C15` supported: MinerU intra-doc hard graph is viable. Same-doc A/B vs LaTeX over 52 overlapping docs gives 90.8% figure/table extraction recall and 84.0% `\ref` reference recall; sampled `regex_reference` edges were 6/6 correct.
- `claim:C16` supported: current MinerU+CLIP+caption xdoc visual edges are recall-only, not strong semantic edges. They should not become hard multi-hop graph edges without a stronger semantic judge.
- `claim:C17` supported: formula edges should use a math-aware encoder, not CLIP text. Probe: CLIP formula similarity std 0.027 vs math model std 0.172.
- `claim:C10/C11` still relevant: graph rerank helps figure/table more than formula; formula gains require real math-aware routing, not surface normalization or NL context injection.

## Failed Ideas / Anti-Patterns

- Do not sell pure CLIP xdoc visual similarity as strong semantic linking; layout/caption artifacts remain.
- Do not do more caption-token TF-IDF threshold tuning as the main research move; 87% caption-zero-overlap makes that a dead end.
- Do not inject generic visual or paragraph text into corpus entries as a blanket fix; prior C8/C11 experiments show style injection can hurt top-10 retrieval.
- Do not return to LaTeX-only graph assumptions; the PDF-first constraint is now central.
- Do not publish graph artifacts as paired delivery data unless chunk/node IDs exactly match the delivered corpus.

## Top Papers / Reusable Methods

- `paper:hessel2019_multilink`: open-source weak supervision via document co-occurrence for intra-doc image-sentence links. Useful if training a linker instead of staying zero-shot.
- `paper:hwang2026_connecting_dots`: VLM direct link judgment; useful because it bypasses degraded captions.
- `paper:wang2026_s1mmalign`: S1-MMAlign dataset/recaption enrichment; useful for abstract+context recaption before CLIP or rerank.
- `paper:tian2026_corank`: coarse-to-fine LLM rerank using document features; useful for scalable xdoc candidate reranking.
- `paper:bsap2024_clip_retrieval_bias`: CLIP score de-biasing; useful for layout-similarity false positives.

## Active Chains

- MinerU hard intra-doc edges validated (`C15`)
  -> PDF-first graph backbone is viable
  -> cross-doc edges become binding constraint (`C16/G10`)
  -> validate `idea:008` on existing 3238 candidates
  -> promote only judged-strong xdoc edges into M4 chain construction.

- Formula CLIP collapse fixed (`C17`)
  -> formula similarity edges are now meaningful
  -> add symbol/Jaccard sanity checks later
  -> keep formula routing separate from visual xdoc rerank.

- Query delivery pushed without exact graph pair
  -> derive an exact companion graph from the delivered corpus if needed
  -> only then publish graph as official training companion.

## Recommended Next Experiment

Run the actual VLM judge over the constructed `idea:008` Phase 0 pack:

1. Use `data/05_eval/idea008_phase0_judge_pack_latest/prompt_batch.jsonl` and attach both image paths per candidate.
2. Add a caption-independent semantic judge: first VLM direct link judgment or recaption+LLM rerank; keep CLIP only as recall.
3. Measure whether strong-edge share rises above the current ~5% without false-positive inflation, especially on degraded-caption edges.
4. If positive, feed only judged-strong xdoc edges into `cross_doc_pairs.py` / M4 chain generation. If negative, keep xdoc visual edges as retrieval candidates only and prioritize exact delivery graph packaging.
