# Research Wiki Log

- 2026-04-18T00:00:00Z: Wiki initialized for `data-process-test`.
- 2026-04-18T00:05:00Z: Added idea pages for explicit rerank, cross-doc summary edges, and Method C query synthesis.
- 2026-04-18T00:10:00Z: Added experiment pages for rebuilt dense baseline, corrected explicit rerank, graph-source audit, and pending cross-doc rerank job.
- 2026-04-18T00:15:00Z: Added claims C1-C4 and linked them to active ideas and experiments.
- 2026-04-18T00:20:00Z: Recorded latest project requirements from `4.16.md` and the later section of `标准录音 57.mp3_20260417_190739_精转文稿.docx`.
- 2026-04-18T00:30:00Z: Added M2 production analysis and experiment tracker under `refine-logs/`, concluding that initial M2 query production can start immediately via the existing batch pipeline.
- 2026-04-18T00:40:00Z: Added full-doc v2 CPU generation sbatch plan and a separate general-query planning note so the project can scale graph-grounded queries without conflating them with universal reader queries.
- 2026-04-18T01:10:00Z: Promoted the new 1040-doc corpus to the sole production corpus, merged partial new-doc element enrichments into `multimodal_elements_v2_production_partial.json` (813 docs / 6707 enriched elements), confirmed zero usable section-enrich overlap on production docs, and submitted full multi-style CPU generation job `61151` using `latex_reference_graph_v2` plus raw element bodies to avoid hallucinated query grounding.
- 2026-04-18T07:00:00Z: Confirmed root cause for C2: intra-doc virtual edges dilute top-rank precision via `static_prior = log(degree)/log(max_degree)`. `same_chunk`/`chunk_sequence` edges raise degree for co-passage distractor nodes, giving them unearned prior weight. Proof: `explicit_only_fixed` vs `all_virtual_fixed` have identical `recall@100=0.8636` but `hit@1` drops 0.484→0.431 and `median_first_hit_rank` worsens 1→2. Virtual edges remain valid for multi-hop graph traversal (different retrieval paradigm). Updated claim C2 with mechanism.
- 2026-04-19T12:00:00Z: Added `exp:20260419_deliverable_420` — 0.6B on v2_chunks hurts R@1 as candidate pool, but using the v2 chunk graph as rerank context yields 0.6B R@1 = 0.2505 (beats 4B baseline 0.2421).
- 2026-04-19T12:10:00Z: Added `exp:20260419_multi_source_stacking` (variant A equal-weight, variant B per-source weighted). Introduced per-source weights, `--merge-combine sum/max`, and `--prior-mode weighted` in `eval_graph_topk_rerank.py`.
- 2026-04-19T12:15:00Z: Added `exp:20260419_cross_doc_bug_fix` — `load_cross_doc_adjacency()` was producing 0 pid-pairs; fix uses edge metadata `source_doc` / `source_section` hints, yielding 5135 pid-pairs and 167 doc-pairs.
- 2026-04-19T12:20:00Z: Added `exp:20260419_typed_crossdoc` — built `typed_crossdoc_edges.json` (16520 edges, figure/formula/table, bbl +0.05 boost). `explicit + typed (w=0.2)` under `static_plus_neighbor` gives R@10 = 0.6406, project high. Added `idea:004` and `claim:C5`.
- 2026-04-19T12:25:00Z: Added `exp:20260419_summary_line_closed` and `claim:C6` — summary virtual nodes produce no strict R@1 + MRR uplift; summary-as-retrieval-uplift line formally closed.
- 2026-04-19T12:30:00Z: Revised `claim:C3` to partially-supported form (R@10 only, after bug fix); superseded at element granularity by C5. `exp:20260418_cross_doc_summary_pending` marked superseded in index.
- 2026-04-19T13:00:00Z: Updated `idea:002` stage from `active_validation` → `pivoted`; summary-as-retrieval closed (C6) but summary nodes retained as cross-doc structural scaffold. Updated `index.md` Project Direction to reflect current virtual-node status and 4.20 open gates.
- 2026-04-19T13:30:00Z: Wrote `refine-logs/EXPERIMENT_PLAN_RETRIEVAL_2026-04-19.md` — full claim-driven plan with B3 (combo R100-R103), B4 (paragraph merge R104-R106), B5 (bbl R107-R108), B6 (QA blocked). Updated `EXPERIMENT_TRACKER.md` with R100-R109 retrieval runs. Added `exp:20260419_combo_plan` wiki node.
- 2026-04-19T14:00:00Z: Split wiki into Track A (graph research) and Track B (query production). Updated `index.md` with two-track structure; added Track B inventory (556 pass queries, 500 target met); created `refine-logs/PRODUCTION_TRACKER.md` (P001–P005); updated `gap_map.md` with [Track A/B] labels.
- 2026-04-19T16:00:00Z: Created `slurm_scripts/32_combo_typed_chunkv2.sh` (B3, R100-R103) — CPU-only, all files exist, submit now. Created `scripts/build_paragraph_chunks.py` (reads paragraph_nodes from chunk_virtual_nodes_v2.json, re-merges with --chunk-size 400/500; tested: 1147 docs, avg 497 words at n=400). Created `slurm_scripts/33_paragraph_merge.sh` (B4, R104-R105) — GPU A5000, uses `build_paragraph_chunks.py` + existing `build_graph_augmented_corpus.py --chunk-file` + `eval_dense_retrieval.py`. Updated EXPERIMENT_TRACKER: R100-R105 all READY.
- 2026-04-19T16:30:00Z: R100-R103 DONE (job 61463). Key result: explicit_only + v1_enriched + static_plus_neighbor = R@10 **0.6522** (new project high, surpasses C5's 0.6406). typed_crossdoc edges with v2chunk corpus hurt R@10 in neighbor mode — confirms C5 scope is v1_enriched only. Added claim C7. Updated C5 (transferred R@10-high title). Updated exp:20260419_combo_plan (verdict: negative on main hypothesis). Added R110 (clean ablation) to Next runs. R104-R105 job 61516 RUNNING on gpu-pro6000-3 (node switched from a5000, nodes down). Added R@2 metric to `eval_dense_retrieval.py` and `eval_graph_topk_rerank.py` (ks: 1→1,2,5,10,100).
- 2026-04-19T17:30:00Z: **Method C 正式废除**，不再追踪长链(4+ hop)路线。LLM enrich 策略确认：不全量 enrich 27209 elements，改为按固定边(hub_pair+adjacent_bridge)思路选 2-3 hop top 25% pairs → 仅 enrich 参与这些 pair 的 ~1000-1200 elements。代码改动：(1) `enrich_hub_candidates.py` 加 `--hub-scores`/`--top-ratio` 参数；(2) `rerun_llm_qc.py` 补 `log_run`；(3) `src/qc/llm_judge.py` ablation 改为单次批量调用，固定 2 次 LLM 调用/query。1040 docs 现状：reference graph ✅ / chunk merge ✅ / element extraction 1040 docs ✅ / LLM enrich 仅 6707 elements（长链路径子集）；gap227 enrich job 61526 RUNNING；top-25% hub pair enrich job 61529 RUNNING（3步 pipeline：map→enrich elements→reinject T/M/C）。
- 2026-04-19T08:00:00Z: **Job 61529 (35_hub_shortchain_enrich) COMPLETED** exit 0，但 Step 2 全部失败（759/759 elements 均 403 Forbidden）。根因：61526 同时占用 company API key，触发并发限制；`hub_candidates_v2_top25.json` 已生成但 0 enriched elements。Step 1 成功：1156 pairs（mapping rate 92.5%，2-hop 736 / 3-hop 420，349 docs covered）；subset file 292 docs / 759 elements 已就绪。创建 `slurm_scripts/35b_hub_shortchain_enrich_retry.sh`（仅跑 Step 2+3，delay 0.3s，incremental）。**等 61526 完成后提交**。**Job 61526 (34_enrich_gap227) 继续运行**，约 1369/4301 elements (~32%) 已完成（08:00 UTC）。
- 2026-04-19T08:25:00Z: **Job 61526 (34_enrich_gap227) COMPLETED**，但 Step 1 LLM enrich 全部失败（4301/4301 均 403）——与 61529 同时跑导致并发冲突。`multimodal_elements_v2_gap227_enriched.json` 写出但 0 enriched elements。Step 2 merge 也因此写出 0 新 enrichment（production_full.json 与 production_partial.json 等价，仍 6707/27209）。
- 2026-04-19T14:30:00Z: **R@10 project high 更新：0.6913**。来源：`rebuilt_20260417/graph_explicit_only_fixed + static_plus_neighbor`，473 queries，qrels = `M4query_v1/qrels.jsonl`（权威）。之前 C7 记录的 0.6522 使用的是 `augmented_v2/qrels_v1.jsonl`（旧本地副本），同配置、同 corpus，qrels 不同导致数值差异。**C7 需要更新**。两套 explicit_only 配置参数（graph_sources, static_weight, neighbor_decay）完全相同，确认是 qrels 文件差异。
- 2026-04-19T14:36:00Z: Job 61647（36_combined_enrich_pairs）提交后发现三个 bug：① 无 coverage gate（enrich 全败也不 exit 1，形成假阳性）；② Step 7 按 pair_id 去重不可靠（pair_id 是 doc_id+顺序计数器，跨 run 不稳定）；③ Step 7 合并丢弃 adjacent_bridge_elements/adjacency 字段。已取消 61647。
- 2026-04-19T14:43:00Z: 提交 **Job 61649（36b_combined_enrich_pairs）** — 三个修复：FIX-1 coverage gate（SC≥100/759，GAP≥500/4301，exit 1 否则）；FIX-2 dedup 改用 (doc_id, frozenset[element_a_id, element_b_id])；FIX-3 保留 adjacent_bridge_elements + adjacent_bridge_adjacency（两包合并，去重）。Gap227 的 467 个候选（hub_scores=0，不能用 top-ratio）单独提取后无 top-ratio 过滤，全量纳入。最终输出 `hub_candidates_v2_combined.json`。

- 2026-04-21T00:00:00Z: **Chunk 革新与评估对齐讨论（含录音整理）**。要点：
	- **Chunk 结构边**需要明确写入文档：当前只有 `chunk↔paragraph/section` 边，没有 `chunk↔element`，这是 mentor 质疑的核心缺陷。
	- **enrich 覆盖范围**需在报告里写清：element enrich 只覆盖 hub 选中节点；section 有 section-level enrich；chunk 当前无 enrich。
	- **负样本策略**：top-11~top-15 仅基于 embedding，相似度最高被排除但未验证“干净性”。
	- **失败机制纠正**：v2 chunks 低分不只是“稀释”，更关键是 **qrels 不认 chunk ID**，chunk 命中不计分；同时 chunk 纯文本导致语义密度低，混合 corpus 时天然偏向 enriched element。
	- **chunk passage 应注入 element 语义**：图像/表格/公式的 enriched_content 应进入 chunk；并补 `chunk↔element` contains 边，便于 graph rerank 传播。
	- **评估方案**：
		- 方案 A（推荐）：chunk 作为检索单元，qrels 走 `elem→chunk` 映射。
		- 方案 B（兼容旧实验）：element 保留，qrels 同时认可 element+chunk。
	- **精确对齐方案**：优先使用 LaTeX 侧的行号/段落定位字段重建 chunk 与 element 归属（比 `context_before` 模糊匹配更稳）。
	- **执行顺序**：先完成 element enrich → 再建 chunk（注入 enriched 内容）→ 可选 chunk summary；评估指标增加 R@2 以匹配双证据任务。
- 2026-04-21T00:20:00Z: 新增 `exp:20260421_api_logging_compliance`，把 `local_api_logger -> /projects/myyyx1/data-process-test/api_logs` 明确记为项目内部铁律；该约束只写入 wiki，不写入给领导看的汇报文档。
- 2026-04-21T09:15:00Z: 提交修正后的 fair pipeline：`62476` graph-only fair eval、`62477` graph-only fair rerank、`62478` trial57 backfill、`62479` enriched fair eval、`62480` enriched fair rerank。随后定位 `62478` 失败原因为 `PYTHONPATH` 在 `set -u` 下未定义，并修复 `37b_trial57_backfill_enrich.sh`。
- 2026-04-21T09:20:00Z: 重提 enriched fair chain：`62482` trial57 backfill enrich、`62483` enriched fair eval、`62484` enriched fair rerank。新增 `exp:20260421_trial57_fairness_repair`，记录本轮对话形成的 fairness 修复、负结论、可复用脚本和当前运行状态。
- 2026-04-21T09:25:00Z: 运行中观察：`62476` build 阶段目前仅覆盖 `460/473` queries（n400/n500 均如此），说明旧的 `473/473` 口径不再可直接复用；`62482` 已进入 backfill API enrich，且 `api_logs/calls/gpt-5.4/2026-04/2026-04-21.jsonl` 与 `api_logs/stats/gpt-5.4/element_enrichment_2026-04.jsonl` 已出现，确认 company API 调用正通过 `local_api_logger -> api_logs` 记录。
- 2026-04-21T10:45:00Z: fair graph-only lane 完成：`62476/62477` 均 exit 0。最终 dense 指标为 n400 `R@1=0.0902 / R@10=0.3348 / MRR=0.2587`，n500 `R@1=0.0967 / R@10=0.3609 / MRR=0.2855`；说明在 rebuilt trial57 口径下，chunk-only 仍明显弱于 element baseline。
- 2026-04-21T11:00:00Z: fair enriched lane 被正式判阻塞：`62482` 虽 exit 0，但 company API endpoint `az.gptplus5.com` 持续 `401 Unauthorized`，backfill 新增 enrich 为 0；`62495` 因 coverage guard (`47.2% < 95%`) 失败，`62496` 被取消。
- 2026-04-21T11:30:00Z: partial-overlay exploratory lane 完成：`62553/62554` 均 exit 0。dense 指标：n400 `R@1=0.1500 / R@10=0.5109 / MRR=0.3975`，n500 `R@1=0.1587 / R@10=0.5304 / MRR=0.4196`。n400 rerank 最佳 `seq+exp / static_prior` 达 `R@10=0.6598 / MRR=0.5168`，`seq+exp / static+neighbor` 达 `R@1=0.2261 / MRR=0.5297`。
- 2026-04-21T12:00:00Z: 新增 `scripts/eval_bm25_retrieval.py` 与 `slurm_scripts/37e_chunk_bm25_eval_newchunk.sh`，并在本地对新 chunk 语料完成 BM25 评测。fair：n400 `R@10=0.3359 / MRR=0.2574`，n500 `R@10=0.3652 / MRR=0.2696`；partial overlay：n400 `R@10=0.5522 / MRR=0.4831`，n500 `R@10=0.5870 / MRR=0.5054`。结论：partial overlay 下 BM25 当前强于 dense，n500 consistently 优于 n400。
- 2026-04-21T14:10:00Z: 提交 `exp:20260421_crossdoc_gold57_validation`（Job `62671`，62649 因 `sentence_transformers` 新版不再接受 `encode(..., max_length=)` kwarg 挂掉，已在 `build_crossdoc_gold57.py` 里改用 `model.max_seq_length`）。生成测仍走 element（M4query_v1 qrels 不认 chunk），chunk 仅参与检索图：`build_crossdoc_gold57.py --include-chunks` 在 gold-57 上生成 19619 条跨文档边（figure 7411 / formula 8864 / table 232 / chunk 3112），覆盖 84.7% BBL pair（72/85，13 对因 threshold=0.7 过滤）。`validate_and_project_crossdoc.py` 把 3112 条 chunk 边通过 `chunk_contains_element` 投影回 element 空间，得到 845 条 `chunk_mediated` 边（fanout mean=0.27 / p95=2 / max=20，保守）。9 个 rerank config 下 `explicit_only` 仍是本地 SOTA（neighbor `R@10=0.6892 / MRR=0.6006`；static `R@1=0.2431 / MRR=0.6372`）；所有 `crossdoc + explicit` 组合均净负，`w=0.2 < w=0.5`，cite-boost 0.05 量纲下无感。结论：跨法（BBL+embedding+chunk 投影）机制正确、数据无爆炸，但当前作为 rerank 信号对 M4query_v1 是净负；下一步若救 crossdoc 需缩到 sim>0.85 + top-3 + cite-only 的高置信子集。
- 2026-04-22T04:00:00Z: 新增 `exp:20260422_decoupling_round1`（基础设施，非检索实验）。综合两位同事给的 Plan A / Plan B：骨架取 B（`src/` 库 / `experiments/` 一次性 / `scripts/` 薄壳三层），吸收 A 的 `src/api/llm.py` 和 `src/retrieval/{bm25,metrics}.py` 两处拆分。落地 Phase 0/0.5/1/2/4/7：写 SOT `docs/DECOUPLING_PLAN_2026-04-22.md` 固化 R1–R6 硬规则（R1 `local_api_logger` 不得绕过 / R2 `log_run()` 唯一记账口 / R3 每 Phase 一 commit + pytest / R4 不边抽边改 / R5 相对路径 / R6 god script 三步走）；修 `src/qc/__init__.py` phantom re-export（`judge_evidence_necessity` → `judge_single_element_batch`）建绿基线；删 `src/pairing/endpoint_anchor.py` 575 行死代码（0 个外部 importer）；新增 `src/io/{jsonl, paths}` + `src/cli/common_args`；`src/api/__init__.py` 273 行 `git mv` 到 `src/api/llm.py`，`__init__` 改 re-export shim；`src/retrieval/__init__.py` 92 行拆为 `bm25.py` + `metrics.py`；建 `experiments/__init__.py` stub。`pytest tests/ --ignore=tests/test_negative_sampling.py` 107 全绿。R1 grep 验证：`yunwu.ai` 命中全部在 docstring 或 `wrap_requests_call()` 调用链上，零裸 HTTP 调用。零行为变化，所有既有 `from src.api import ...` / `from src.retrieval import ...` 依然可用。Round 2（迁 24 处 load_jsonl、实验脚本物理迁移 + slurm 同步、`rerun_llm_qc.py` 补 log_run、CodeQL clear-text 两处）和 Round 3（拆 templates 899 / topology 2019 / generation 1719+1388）已挂账。

- 2026-05-02T08:00:00Z: **Mentor 会议（录音 60）** — DPT 下一步方向确认。三个核心要求：
	1. **术语统一**：paragraph 就是 text element，与 figure/table/formula 平级，不要分两套名词。代码里内部叫什么都行，但写文章时只认 element 这一个概念。
	2. **Chunk 公平性验证**：先统计一个 chunk 平均覆盖几个 element，再讨论 chunk 适不适合做检索单元。需要区分"生成侧"（无 chunk，query 的 evidence 仍按 element 标注）和"检索侧"（chunk 可参与检索图）。
	3. **分离式检索**：文本 query 找文本 chunk，图/表 query 用多模态 embedding 独立检索。不要一股脑把所有模态塞进同一个 dense 空间。
	4. **虚拟边暂缓**：mentor 试过，信息压缩太狠，当前不作为主卖点。
- 2026-05-02T08:10:00Z: **chunk→element 覆盖分析完成**（`scripts/analyze_chunk_element_coverage.py`）。在 M4query_v1 57 docs 上：964 chunks，平均每 chunk 含 1.94 elements。52% 的 chunk 仅含 1 个 element。关键发现：75% 的 query 需要命中 2 个不同 chunk 才能覆盖双 evidence（仅 10/473 即 2% 的 query 两个 evidence 落在同一 chunk）。这解释了 chunk 检索天然的劣势——检索单元变稀疏（964 chunks vs 1798 elements），且双证据分散。R@1 时只能命中一个 element 所在的 chunk。
- 2026-05-02T08:20:00Z: 新增 `exp:20260502_chunk_element_coverage` 和 `exp:20260502_split_modality`。提交 Job 66036（split_modality eval，0.6B + 4B，M4query_v1）。新增 `scripts/analyze_chunk_element_coverage.py`、`slurm_scripts/41_split_modality_eval.sh`。
	- 2026-05-02T08:51:00Z: **split_modality 三连挂 + 最终结果**。① Job 66036 OOM（没设 max_seq_length，默认 32K token 炸 A6000 48GB）→ ② Job 66041 faiss 没装 → ③ Job 66048 跑通。
	  **纯文本 split 是伪命题**：baseline R@10=0.4767（混合索引，4B），split 后跌到 0.3235。table 和 formula 的 R@10 **直接归零**——4095 条非文本 passage 的 content 全是 `[Image: xxx.jpg]` 占位符，Qwen3-Embedding（纯文本）编码这些 = 随机噪声。
	  **根因确认**：1095 条 figure passage 有 `image_path` 但无 caption/description。分离式检索要想 work，必须换 VL embedding 模型直接读图像。
	- 2026-05-02T08:55:00Z: **下载 Qwen3-VL-Embedding-2B**（HuggingFace 2.3M downloads / 4.0GB），Qwen2.5-VL-7B-Instruct 补齐缺失分片（model-00001/2-of-00005）。SVP G1 Pilot (66047) 因模型不完整挂掉，现已修复。
	- 2026-05-02T09:15:00Z: **VL 版 split_modality 准备 & 提交**：
		- **图/表 image 覆盖率**：figure 852/1095 (77.8%)，table 228/237 (96.2%)。Table 的 `image_path` 前缀是 `data/00_raw/mineru_output/{doc_id}/`，需 strip 后才能解析到 `documents/{doc_id}/mineru/`。
		- **Formula 无图像**：1253 条公式 text 是 LaTeX 源码 `[FORMULA] D (M x, M y) \leq d (x, y). \tag {1}`，纯文本编码正确。
		- **243 条 figure 无 image_path**：但 text 是实际描述文本（非 `[Image: ...]` 占位符），可 fallback 文本编码。
		- **新建文件**：`scripts/eval_split_modality_vl.py`（智能路径解析 + VL 编码）、`slurm_scripts/41_split_modality_vl.sh`。
		- **Job 66105** 已提交（gpu-a6000-1），Qwen3-VL-Embedding-2B，2048-dim 统一多模态空间。
		- ⚠️ Qwen3-VL-Embedding-2B 加载时 language_model 权重全部 "newly initialized"（视觉 encoder 正常），text encoding 质量待验证。
		- ⏳ **SVP G1 Pilot 需重提**：Qwen2.5-VL-7B-Instruct 分片已补全（5/5），模型就绪。
		- ⏳ **Job 34 enrich 产物的 gap227 数据待分析**（66048 结果中未涉及）。
- 2026-05-02T11:30:00Z: **Job 66105 失败 → 修复 → Job 66114 RUNNING**。根因：Qwen3-VL processor 把 ~1247 vision tokens 截到 `max_seq_length=512` (496)，导致 text 侧 image-token 占位符与 input_ids 数量不匹配（`Mismatch in image token count between text and input_ids. Got ids=[496] and text=[1247]`）。修复：[scripts/eval_split_modality_vl.py](scripts/eval_split_modality_vl.py) 拆分 `--text-max-length=512`（query/text passages）和 `--image-max-length=4096`（image encoding），仅图像 pass 临时上调 `model.max_seq_length`，避免 text 侧多吃显存。Job 66114 已通过原失败点：1080 张图 119s 编完（0.11s/img），1729 文本 passage 编码中。一致性保留：M4query_v1 corpus/qrels/queries 不变，merge_configs (equal/prop/boost_formula/boost_figure) 与 Job 66048 (text-only baseline R@10=0.4767) 一致，可直接对比"VL 编码是否救活 figure/table 的 0.0 召回"。
- 2026-05-02T12:25:00Z: **Job 66114 COMPLETED → 灾难性失败**。详见 [exp:20260502_split_modality_vl_failed](research-wiki/experiments/20260502_split_modality_vl_failed.md)。结果：split_VL_2B baseline R@1/5/10/100/MRR = 0.0011/0.0011/0.0021/0.0074/0.0028（vs split_4B_text 0.4767 / vs v1_enriched_4B 0.6195）。per-modality R@10：figure 0.0093 / table 0.0 / formula 0.0。根因坐实：Qwen3-VL-Embedding-2B 的 HF checkpoint **只发布了 vision encoder**，language_model 28 层全部 "newly initialized"（启动时已警告），导致 query 和 text passage 编码出来是噪声，random retrieval 上限 ≈ k/N ≈ 100/2809 ≈ 0.0036，与观测 0.0021 完全吻合。**结论**：这个 checkpoint 不可用于任何 text→multimodal retrieval。下一步需师兄拍板：(1) 换真正多模态 embedding 模型（jina-clip-v2 / BGE-VL / GME-Qwen2-VL）；(2) caption-then-text-embed（用 VLM 给图/表生成描述，再用 Qwen3-Embedding-4B 编码统一空间，复用 enrich_elements_modora 的 enriched_content 通道）；(3) hybrid + linear projection 对齐两套 embedding 空间。3 个选项已写入 exp 节点末尾。

- 2026-05-03T03:30:00Z: **录音 60 完整 todo 提取 + 完成度核查**。
  - 之前 wiki 只摘了 4 点高层要求，重读 `/projects/myyyx1/标准录音 60.mp3_20260502_134902_精转文稿.docx` 提取了完整 18 条 todo（A 文档/术语 3 + B 数据/匹配 4 + C 实验 7 + D 工作方式 3）。整体完成度 ~26%。详见 [exp:20260503_mentor_recording60_full_todo](research-wiki/experiments/20260503_mentor_recording60_full_todo.md)。
  - User 5/3 决定 A1/A2/A3（术语统一进代码、`文档建图.md` 重写、AI 命名换大白话）+ D1（整理 todo 发师兄）交给别人处理；本 session 仅记录不动手。
- 2026-05-03T06:30:00Z: **Hybrid rank fusion 实验计划完成**。写 [refine-logs/EXPERIMENT_PLAN_HYBRID_20260503.md](../refine-logs/EXPERIMENT_PLAN_HYBRID_20260503.md)（初版，后被修正）。新增 [exp:20260503_hybrid_rank_fusion](experiments/20260503_hybrid_rank_fusion.md)。Tracker 新增 R120-R127。
- 2026-05-03T08:00:00Z: **VL enrich comparison + Cross-doc citation 实验计划**。User 确认方向后写入两个 plan：(1) [VL enrich comparison](../refine-logs/EXPERIMENT_PLAN_VL_ENRICH_20260503.md) — enrich_only text (4B) vs raw image (VL-2B)，isolate VL's irreducible value，~25 min GPU；(2) [Cross-doc citation pipeline](../refine-logs/EXPERIMENT_PLAN_CROSS_DOC_20260503.md) — element-level citation mapping → cross-doc query generation → retrieval eval，5 步 pipeline，~1h + 20 min GPU + $3-5 API。Wiki 新增 [exp:20260503_vl_enrich_comparison](experiments/20260503_vl_enrich_comparison.md) 和 [exp:20260503_cross_doc_citation](experiments/20260503_cross_doc_citation.md)。Tracker 新增 R130-R133 + R140-R143。
- 2026-05-03T07:00:00Z: **事实修正 v2**。User review 发现两个关键错误：(1) Track B enrich 覆盖率写错——production_full 实为 10988/27209=40.4%，gap227 实为 4283/4301=99.6%，非"全线阻塞"；(2) Hybrid plan 核心假设有偏——per-modality 证据显示 4B text 在 figure/table 上强于 VL（split figure 0.71 vs 0.54, mixed table 0.50 vs 0.02），VL 仅在 formula mixed 有微弱优势（0.34 vs 0.30）。Hybrid plan 从固定"figure/table→VL"改为 5-config routing ablation（B1: 5 routing configs, B2: per-modality encoder comparison, B3: mixed vs split index, B4: RRF k sensitivity）。修正了 [data_architecture.md](reference/data_architecture.md)、[EXPERIMENT_PLAN_HYBRID_20260503.md](../refine-logs/EXPERIMENT_PLAN_HYBRID_20260503.md)、[exp:20260503_hybrid_rank_fusion](experiments/20260503_hybrid_rank_fusion.md)、EXPERIMENT_TRACKER.md。37f consistency check 改为硬 gate（<99% exit 1）。
- 2026-05-03T06:00:00Z: **数据架构文档 + P1 bug 根因定位 + Job 61649 状态确认**。写了 [ref:data_architecture](reference/data_architecture.md)，清晰分离 Track A（实验轨）和 Track B（生产轨）。P1 chunk-element 边不一致根因定位：`build_chunk_corpus.py` 的 eval qrels 用 `paragraph_chunks_n400.json`（v1 build），`eval_chunk_graph_rerank.py` 的 chunk_contains_element 边用重建的 `paragraph_chunks_n400_trial_full.json`（另一次 build），两次 build 的 paragraph merging 不同导致 chunk_id→element 分配不一致，图传播跑在错误映射上。Job 61649 确认 4/19 FAILED（API 403）。Enrich 全线被 API key 问题阻塞。
- 2026-05-03T04:00:00Z: **C1 完成：per-query chunk→element recall 分析**。
  - 写 [scripts/analyze_chunk_query_element_recall.py](scripts/analyze_chunk_query_element_recall.py)（mentor 真正问的"recalled chunk 实际含几个 element"），跑 4 个 lane（n400/n500 × fair/partial-overlay）。
  - 结果（n500 partial-overlay 最优 lane）：avg recalled@10 = 0.95 / 1.77，elem R@10 = 0.530，chunk hit rate@10 = 0.678 → **chunk R@10 vs element R@10 有 ~15pp 鸿沟**，K=1 时 71% query zero-recall。坐实 mentor 怀疑 chunk 稀释信号。enrich overlay 给 +17pp，但天花板停在 53%。
  - **副产品 P1 bug**：`paragraph_chunks_n400_v2.json` 的 `chunk_contains_element` 边和 eval-time `chunk_corpus_*/qrels.jsonl` 的 chunk_id **对全部 57 个 M4query_v1 文档 0% 一致**。两套独立 build 流程命名空间冲突，依赖 v2 文件 chunk-element 边的代码可能跑在错前提上。
  - 详见 [exp:20260503_chunk_query_element_recall](research-wiki/experiments/20260503_chunk_query_element_recall.md) + 跨 lane 总表 `data/05_eval/chunk_query_element_recall_summary.md`。
- 2026-05-03T04:10:00Z: **Qwen3-VL-Embedding-2B 根因复查 + 修正 5/2 误诊**。
  - 5/2 把 R@10=0.0021 解释成"checkpoint 只发布 vision encoder"。复查 HF 官方页面 + 本地 safetensors 后**该解释是错的**：单文件 4.26GB 的 SentenceTransformer 包装，625 个 key 全部 `model.` 前缀，含完整 28 层 `model.language_model.layers.*` 权重。Qwen 官方 PR #19 标题 "making the script compatible with **transformers 5.2+**"。我们环境 `transformers==4.57.6`（vllm + compressed-tensors 钉死该版本），AutoModel 把 `language_model.layers.X.*` 当成 missing key、把 `model.language_model.layers.X.*` 当成 unexpected key，28 层全部 "newly initialized" 但 weights 实际存在。
  - 修复方案：clone minerU env → `qwen3vl_emb` → upgrade `transformers>=5.2,<6` → 重跑 Job 66114。新 slurm: [slurm_scripts/41b_split_modality_vl_t5.sh](slurm_scripts/41b_split_modality_vl_t5.sh)。
  - [exp:20260502_split_modality_vl_failed](research-wiki/experiments/20260502_split_modality_vl_failed.md) 的 Root cause / Verdict 段已更新（保留旧错误解释作历史记录），新 [exp:20260503_split_modality_vl_t5_rerun](research-wiki/experiments/20260503_split_modality_vl_t5_rerun.md) 跟踪重跑。env clone in progress。
- 2026-05-03T04:45:00Z: **VL rerun setup 改为 transformers 5 overlay**。`conda clone minerU -> qwen3vl_emb` 在 cluster FS 上过慢，已停止；改用 `/projects/myyyx1/envs/qwen3vl_tf5_overlay` 通过 `PYTHONPATH` shadow `transformers/tokenizers/huggingface-hub`，不污染 minerU。Import sanity passed: `transformers 5.7.0` + `sentence_transformers 5.4.1`。已更新 [slurm_scripts/41b_split_modality_vl_t5.sh](slurm_scripts/41b_split_modality_vl_t5.sh) 和 [exp:20260503_split_modality_vl_t5_rerun](research-wiki/experiments/20260503_split_modality_vl_t5_rerun.md)。
- 2026-05-03T04:43:00Z: **Job 66243 submitted/running** — `slurm_scripts/41b_split_modality_vl_t5.sh` on `gpu-a6000-1` using minerU + transformers 5 overlay. Output: `data/05_eval/dense_retrieval/split_modality_vl_t5/`; logs: `logs/41b_split_modality_vl_t5_66243.{out,err}`. Waiting for load sanity and final R@10.
- 2026-05-03T04:55:00Z: **Job 66243 failed after clean weight load; Job 66244 resubmitted**。66243 已确认 `transformers 5.7.0`，625/625 weights loaded，且没有 5/2 的 `newly initialized` warning；随后在 `AutoProcessor` 初始化中因旧 `mistral_common` 缺 `ReasoningEffort` 失败。已将 `mistral-common 1.11.1` 安装到 overlay，并重提 Job `66244`。
- 2026-05-03T05:08:00Z: **Job 66244 failed on overlay pydantic-core mismatch; dependency repaired**。66244 在 sanity import 阶段发现 `pydantic 2.13.3` 需要 `pydantic-core 2.46.3`，但 minerU 旧 core `2.33.2` 被加载。已显式安装 `pydantic-core==2.46.3` 到 overlay，并确认 `ReasoningEffort` import passed。
- 2026-05-03T05:13:00Z: **Job 66248 COMPLETED — Qwen3-VL-Embedding env fixed, but VL split underperforms**。
  - Sanity passed: `transformers=5.7.0`, `mistral_common_ReasoningEffort=ReasoningEffort`, 625/625 weights loaded, no `newly initialized` warning. Thus Job 66114 near-random result was environment mismatch, not broken checkpoint.
  - Results: `split_VL_2B_t5` mixed-index baseline `R@10=0.2579 / MRR=0.3217`; best split (`equal_split`) `R@10=0.2442 / MRR=0.3633`.
  - Comparison: `split_VL_2B_t5 R@10=0.2579` beats failed 66114 `0.0021`, but remains below `split_4B_text R@10=0.4767` and unified `v1_enriched_4B R@10=0.6195`.
  - Per-modality: figure improves (`0.4112 → 0.5397` R@10), table remains near-zero, formula collapses under split (`0.3352 → 0.0000`). Conclusion: use VL lane for figure/table only after table audit; keep formula/text in Qwen3-Embedding-4B text lane and merge by rank fusion rather than raw score.

- 2026-05-03T09:00:00Z: **Dense ceiling failure profiling plan 落地，交第三位 coder 助手执行**。三轮评估收敛：(1) Plan 1 (VL enrich-only) 是 known answer 不跑；(2) Plan 2 (cross-doc citation) 保留为副线不主攻；(3) 真正瓶颈是 dense R@100=0.8636，121 个 partial+zero query 是失败池。第二位助手 spot-check 给出关键发现：**missed qrel modality 分布 formula 49.6% / figure 26.4% / table 24.0%**，formula 是漏网主体，重塑了决策树（增加 R1: math-aware encoder swap 分支）。计划 30 min wall-time（5 min 检查 + 15 min GPU + 10 min 决策），输出 `refine-logs/CEILING_DECISION_20260503.md` 单一推荐。详见 [refine-logs/CEILING_PROFILING_PLAN_20260503.md](../refine-logs/CEILING_PROFILING_PLAN_20260503.md) 和 [exp:20260503_failure_profiling](experiments/20260503_failure_profiling.md)。
- 2026-05-03T11:15:00Z: **Failure profiling Phase B/C COMPLETED — decision = R2 (cross-encoder rerank top-500)** (slurm 66324, ~10 min on a6000). Phase A gate passed: formula passages are clean LaTeX (`[FORMULA] $$...$$`), no corpus-side degradation. Full-corpus dense retrieval (top-k=2809) on M4query_v1 / v1_enriched reproduces the 4/17 baseline exactly (R@10=0.6195 / R@100=0.8636). T6 rank-bucket distribution of 129 missed qrels: **(100,500] = 89 (69.0%)**, (500,2000] = 34 (26.4%), >2000 = 6 (4.7%). T7 modality × bucket: formula 42/21/1, figure 25/7/2, table 22/6/3 — formula is **not** rank-collapsed (form_high = 0.016), so R1 (math-aware encoder) is rejected. r_low ≥ 0.60 triggers R2. Recommendation: rerank dense top-500 with BAAI/bge-reranker-v2-m3 (alt: Qwen3-Reranker-4B), success bar R@10 ≥ 0.72 vs ceiling 0.6913, ~30 min A6000, $0. Decision report: [refine-logs/CEILING_DECISION_20260503.md](../refine-logs/CEILING_DECISION_20260503.md).
- 2026-05-03T11:30:00Z: **CEILING_DECISION post-audit sanity checks appended**. (S1) Passage length: 51/2809 (1.8%) exceed BGE-reranker-v2-m3's 8K context — next pilot must truncate before scoring. Queries are short (p90=204 chars). (S2) R@K from `full_ranking.jsonl`: R@200=0.9080, **R@500=0.9577**, R@1000=0.9810. Stretch R@10≥0.75 needs cross-encoder to promote 78.3% of in-pool gold to top-10; primary R@10≥0.72 needs 75.2%. (S3) Confirmed the strong baseline to beat is graph rerank R@10=0.6913, not dense 0.6195; recommend cross-encoder × graph-static-prior linear fusion as the follow-up. Self-consistency check (independent assistant) reproduced T6/T7/decision-var arithmetic exactly.
- 2026-05-03T13:30:00Z: **CE rerank pilot (BGE-reranker-v2-m3) FAILED primary bar — R2 partially invalidated** (slurm 66349, ~28 min A6000). R@10 collapsed from dense 0.6195 to **0.4482 (−17.1pp)**, MRR from 0.612 to 0.371, vs target ≥ 0.72 and ceiling 0.6913. Diagnosis: BGE-reranker-v2-m3 is severely text-biased — top-1 modality dist shifts from {figure 265, table 115, formula 67, text 26} (dense) to {**text 348**, figure 87, table 29, formula 9} (CE). Per-modality R@10: formula −23pp, table −19pp, figure −13pp. Cause: trained on natural-language QA, scores `[FORMULA] $$...$$` and `[Image: ...]` strictly below any NL paragraph with surface overlap. **RRF(dense, CE, k=20)** rescues marginal gains: R@10=0.6258 (+0.5pp vs dense, still −6.5pp vs graph 0.6913), R@100 = **0.8869 (+2.3pp)**. R2's "any cross-encoder" assumption is wrong for this multimodal corpus; BGE-reranker-v2-m3 rejected. Recommended next: F1 = Qwen3-Reranker-4B (same family as encoder, ~45 min A6000), F2 = fix the corpus enrichment bug (figure/formula passages need NL caption concat, ~1h+10min), F3 = HyDE if F1+F2 fail. Full writeup: [exp:20260503_ce_rerank_bge](experiments/20260503_ce_rerank_bge.md).

- 2026-05-03T14:30:00Z: **F1/F2/F3 优先级反转 — F2 (corpus enrichment fix) 提为 P0**。直接核验 `corpus_v1_enriched.jsonl` × `multimodal_elements_enriched.json`：63.5% (695/1095) 的 figure passage 的 text 是 `[Image: xxx.jpg]`，**其中 42.2% (293/695) 在 enrichment source 里有完整 enriched_content，被 corpus build 的键映射 silently 丢掉**。BGE rerank 把它们压低是对的（作为 NL 文本就是垃圾），换 reranker (F1) 大概率重蹈覆辙。F2 的杠杆覆盖整个下游（dense / graph / CE / RRF 全部受益），且 enrichment 数据已经存在不需要 LLM 调用。计划 ~75 min wall-time（15 min 诊断 + 10 min 修+重建 + 10 min GPU 重 baseline + 30 min GPU 重 rerank + 10 min 决策），决策树 D1–D5 路由到 lock-in / F1 / 换 reranker 家族。详见 [refine-logs/CORPUS_ENRICH_FIX_PLAN_20260503.md](../refine-logs/CORPUS_ENRICH_FIX_PLAN_20260503.md) 和 [exp:20260503_corpus_enrich_fix](experiments/20260503_corpus_enrich_fix.md)。交第四位 coder 助手执行。

- 2026-05-03T15:30:00Z: **F2 corpus enrichment fix EXECUTED — verdict D5 antipattern, NOT promoted** (jobs 66371/66384, ~25 min A6000 total). Phase A diagnostic confirmed two independent bugs: (i) `load_enriched_index` only parsed nested `pair.element_a.element_id` layout — current hub files use flat `element_a_id` keys (0 entries) and the MODORA `multimodal_elements_enriched.json` (1285 elements, `documents.elements` layout) was never loaded; (ii) `build_element_text` priority was OR (mutually exclusive) — when MODORA enrich was present it skipped the graph caption/context branch. Buckets: D1=402 (no MODORA), **D2=293** (silently dropped — matches plan), D3=400 (already enriched). Phase B produced two variants: `corpus_fix_v1` (replace, mean fig len 405) and `corpus_fix_v2` (additive: visual + paper context, mean fig len 683). Phase C dense + graph rerank delta vs anchor (rebuilt_20260417): | metric | anchor | fix_v1 | fix_v2 | dense R@10 0.6195 / 0.5106 (−10.9pp) / **0.5888 (−3.1pp)**; dense R@100 0.8636 / 0.7569 / 0.8436 (−2.0pp); graph_explicit_only static_plus_neighbor R@10 0.6913 / 0.5888 / **0.6860 (−0.5pp)**. **Phase D NOT triggered** (R@100 Δ < +2pp gate failed). Mechanism: MODORA visual descriptions are domain-detached ("Histogram of small-valued metric") whereas M4query_v1 queries are paper-domain text-style — same direction as the BGE-CE text-bias. Code state: `DEFAULT_ENRICHED_FILES` reverted (no MODORA by default); latent-bug fixes for `load_enriched_index` documents-format branch and `build_element_text` additive priority kept (no-ops under default args). Recommendation forwarded: do NOT pursue corpus-level visual enrichment for text-style retrieval; higher-EV next is multimodal CE on the anchor corpus, or explicit late-fusion vision-language lane. Full writeup: [refine-logs/CORPUS_FIX_DECISION_20260503.md](../refine-logs/CORPUS_FIX_DECISION_20260503.md) and [exp:20260503_corpus_enrich_fix](experiments/20260503_corpus_enrich_fix.md).

- 2026-05-03T16:00:00Z: **战略综述 — three-experiment convergence pattern + 新 claim C8 入库 + 14:30 UTC 假设正式撤回**。三个独立实验（`exp:20260503_failure_profiling` / `exp:20260503_ce_rerank_bge` / `exp:20260503_corpus_enrich_fix`）现已收敛到同一组事实，重新校准下一步方向：
  - **F1**（已撤回的假设）："corpus 数据 bug 是 ceiling 主因" — F2 D5 直接证伪。693/1095 figure 用 placeholder text 不是 retrieval 瓶颈；graph rerank 早就通过 caption + context_before/after fallback 把这些 passage 检索到了。
  - **F2**（新 claim C8）：MODORA 视觉描述对 text-style retrieval 净负。BGE-CE text-bias 与 fix_v1/v2 dense 回退方向一致——M4query_v1 query 是 paper-domain 文本式，**模态忠实但 domain-detached** 的描述会稀释信号，无论是 corpus 端注入还是 reranker 端打分都失败。
  - **F3**（被强化）：graph rerank 是真正承重墙。fix_v2 dense 跌 −3.1pp R@10，但 graph rerank 仅跌 −0.5pp（0.6913 → 0.6860）。Graph 吸收 corpus 质量方差，是当前 0.6913 上限的稳定来源，不是 corpus。
  - **F4**（唯一正向信号）：RRF(dense, CE) **R@100 +2.3pp** 仍是项目首次也是唯一的 ceiling 移动证据。R@10 没动是因为 BGE 把图/表/公式排得更靠后，但召回端确实补到了。这个信号被 F2 的喧嚣短暂掩盖，现在要拎回来。
  - **重新洗牌后的 shortlist**（按 EV 排序）：
    1. **CE on anchor corpus + non-text-biased reranker**：换 reranker 家族（Qwen3-Reranker-4B / Cohere rerank-3.5 / Voyage rerank-2），保持 anchor corpus 不动，看能否把 RRF R@100 +2.3pp 转化为 R@10 lift
    2. **Late-fusion vision-language lane**：split modality 的 figure lane VL 已能到 R@10 0.5397，与 4B text 0.7110 的 gap 可能用 RRF 关闭，且**不需要改 corpus**
    3. **HyDE query rewriting**：query 变 paper-style 文本，缩小 query-passage style gap，与 dense encoder 配合，绕开 reranker 的模态偏置问题
  - **下一步建议**：把 #1 写成新 plan 交第五位 coder 助手，复用 `slurm_scripts/45_ce_rerank_bge_v2m3.sh` 改 reranker model id 即可，~45 min A6000，$0。新增 [claim:C8](claims/C8_modora_visual_enrichment_net_negative.md)，在 14:30 UTC log entry 已记录的 P0 假设正式标注为撤回。

- 2026-05-03T17:20:00Z: **Qwen3-Reranker-4B autopilot EXECUTED — option A 证伪，无 fusion 突破 0.6913** (autopilot run 20260503T160617Z, jobs 66395/66401/66405/**66408**, ~75 min A6000, model cache 7.6GB)。延续 16:00 UTC shortlist 的 #1 推荐，跑了 anchor corpus + Qwen3-Reranker-4B + 全套 fusion 消融。Headline: dense 0.6195 / graph **0.6913** / ce alone 0.5613 / rrf_graph_ce_k10 **0.6702 (−2.1pp vs graph)**。**关键发现**：Qwen3 跟 BGE 模态偏置完全相反——top-1 选择 BGE = {**text 348**, figure 87, table 29, formula 9}（偏 NL）vs Qwen3 = {text 36, figure 144, table 45, **formula 248**}（偏 LaTeX）。两个 reranker 各偏一个模态，**正好相反**，但都在 R@10 上输给 graph。R@100 上 BGE rrf 给 +2.3pp 这个唯一正向信号 Qwen3 没复现 (Qwen3 rrf_dense_ce R@100 = 0.8594, **−0.4pp** vs dense)，说明那是 BGE-specific 行为不是 generic CE 现象。Per-modality R@10：graph 在 figure (0.7453) / table (0.7257) / formula (0.4972) 三模态全赢，无 fusion 三模态同时拉过 graph。**含义**：Option A（换 reranker 家族）失败；Cohere/Voyage 等同类 reranker 预期收益被削弱（同样会有自己的偏置）。可信 shortlist 收缩到：**B** late-fusion VL lane (graph + VL 而非 graph + CE)、**C** HyDE query rewriting（从 query 端缩 modality-style mismatch）、**新 D** 重新审视 0.6913 是否真 ceiling（M4query_v1 modality 分布严重不均：figure 56% / table 24% / formula 14% / text 5.5%，mentor C6 要的 10/10/10/10 balanced smoke test 至今未做）。详见 [exp:20260503_qwen3_rerank_fusion](experiments/20260503_qwen3_rerank_fusion.md)。

- 2026-05-04: **Wiki 静默期** — 无新 commit / 实验。User 5/3 决定 5/4 那周请假后台跑分析（mentor todo D2，✅ 在跑）。

- 2026-05-05T现在: **Wiki 状态盘点 + 战略校准回到 mentor 录音 60**。盘点发现 5/3 17:20 Qwen3 实验在 wiki 缺失，已补 [exp:20260503_qwen3_rerank_fusion](experiments/20260503_qwen3_rerank_fusion.md)。回看 mentor 录音 60 的 18 条 todo（[exp:20260503_mentor_recording60_full_todo](experiments/20260503_mentor_recording60_full_todo.md)），核心未做项指向同一方向：**C6 小冒烟测试集（10 文本/10 图/10 表/10 公式 = 50 query）至今未建**，而当前 0.6913 ceiling 是在 modality 分布严重不均的 M4query_v1 上测的（figure 56% / table 24% / formula 14% / text 5.5%）。结合 BGE/Qwen3 各偏一个模态的发现，**怀疑 ceiling 形状本身是 artifact**——graph rerank 在 figure/table 上 +5pp 的优势可能就是因为 figure 占评测 56%。下一步建议先做 C6 balanced smoke test，让所有现有系统（dense / graph / BGE / Qwen3 / VL）在干净的 modality 平衡测试上重新出牌，再决定是否走 B/C/D。

- 2026-05-05T现在+10min: **Smoke50 balanced audit plan 落地，交下一位 coder 助手执行**。计划 ~90 min wall-time，$0 LLM，~10 min GPU（且条件触发）。核心思路：**所有主流系统都已经在 M4query_v1 全集跑过，直接 slice 出 50 query × 4 modality 重算 metrics 即可，几乎不需要新计算**。Phase A stratified-sample，Phase B slice ranking，Phase C 条件 VL fusion，Phase D 决策门 S1–S5（ceiling 真假 / route-aware / scope 收紧 / text-specific 失败 / mixed），Phase E 收尾。同时关闭 mentor 录音 60 C6 todo（❌→✅）。详见 [refine-logs/SMOKE50_BALANCED_PLAN_20260505.md](../refine-logs/SMOKE50_BALANCED_PLAN_20260505.md) 和 [exp:20260505_smoke50_balanced_audit](experiments/20260505_smoke50_balanced_audit.md)。 战略含义：如果 S3 命中（0.6913 是 figure-heavy artifact），需要给 C1/C5/C7 全部 claim 加注 modality scope，避免 paper 里过度泛化。

- 2026-05-10T08:00:00Z: **BCD 全部分阶段执行（user 5/10 directive）**。一个 session 推进 mentor 录音 60 的 14 项 BCD todo，整体完成度 32% → **71%**（+39pp）。
  - **C6 smoke50 执行（核心）**：S2 verdict 命中。smoke50 graph R@10 = 0.7100 vs full graph 0.6913 偏差 +1.87pp（< 5pp 阈值），ceiling 是真的、不是 figure-heavy artifact。但 graph 增益是 modality-selective: figure +10.3pp / table +8.3pp / **formula +0pp**。新 [claim:C10](claims/C10_graph_rerank_modality_selective.md) 入库要求 paper 加 modality scope。
  - **关键现实修正**：M4query_v1 qrels 完全没有 text 元素（figure 218 / formula 138 / table 117 / text 0）。BGE pilot top-1=text 是 reranker 错答 348/473，不是 ground truth。Mentor "10 text / 10 figure / 10 table / 10 formula" 在 v1 上不可执行，调整为 17/17/16。
  - **B2 mineru→latex 元素级匹配率审计**：figure 49.7% / table 67.3% / formula **0.0%**。User 印象 50% 与 user-2 92% 都对——讲的是不同粒度（doc 级 98.2% vs element 级 49.7%）。
  - **B1 Phase 1 (LaTeX line_no 提取 + formula content matching)**：写 `scripts/extract_element_latex_lineno.py`，把 formula match rate **0% → 41.2%**（content-jaccard matching 绕过 label key 通道）。Phase 2（重建 chunk 边）下次 session。
  - **B3 多模态元素文档**：写 [ref:multimodal_element_taxonomy](reference/multimodal_element_taxonomy.md) — paragraph=text element 与 figure/table/equation 平级，inline math 不计 element。
  - **C2 chunk 信号稀释结论 finalize 成 [claim:C9](claims/C9_chunk_dilutes_double_evidence_signal.md)**。
  - **B4 / C5 API 状态确认 BLOCKED**：company API endpoint `az.gptplus5.com` 自 4/21 起 401，已 19 天 dead。OpenAI 直连可作 fallback 但需 mentor 同意（成本 ~10x）。详见 [B4_C5_API_STATUS_20260510](../refine-logs/B4_C5_API_STATUS_20260510.md)。
  - **D1 mentor todo list 草稿就绪**：[MENTOR_TODO_DRAFT_20260510](../refine-logs/MENTOR_TODO_DRAFT_20260510.md)，user 审后发即可。
  - **未跑**：A1/A2/A3 仍 handoff，C5 deferred (与 C8 矛盾)，B4 API blocked，B1 Phase 2 chunk 重建下次 session。

- 2026-05-10T11:30:00Z: **B1 Phase 2 执行完成（user 5/10 second directive）**。重建 chunk-element 边用 LaTeX 行号 + 6 配置 graph rerank ablation + smoke50 重新 slice。**Hypothesis 部分证伪**。
  - **设计**: paragraph 文本→.tex 行号匹配（25.3% match rate, 53/57 docs）→ chunks 拿 line_range → elements 按 line_no remap position_idx → graph rerank 6 配置（origpos vs lineno × 3 graph modes）→ smoke50 slice
  - **拓扑确实变了**: 24/49 formulas in `1104.3913` chunk membership 改变；audit 总计 kept=20 / added=1130 / removed=529 — 旧 fuzzy 匹配的 chunk-element 边和新 line_no 几乎完全不重合
  - **但 R@10 没动**: explicit-only ceiling 0.7100 完全不变（line_no 和 origpos 同分），因为 explicit graph 用 hub bridge，不经过 chunk-element。Per-modality 唯一可观察改善：table on explicit+virtual +2.78pp（0.6111→0.6389），其他全 0pp
  - **关键发现**: **6 个独立 configurations 全部在 formula 上 ≤ 0.5600**（dense, graph explicit-only orig, graph explicit-only lineno, graph explicit+virtual orig, graph explicit+virtual lineno, Qwen3-CE）。formula 0.56 不是 graph-topology 问题，是 **dense encoder 在 LaTeX content 上的 representation ceiling**
  - **新 [claim:C11](claims/C11_formula_ceiling_is_dense_encoder_bound.md) 入库**: "Formula retrieval ceiling is dense-encoder bound, not graph-topology bound"
  - **[claim:C10](claims/C10_graph_rerank_modality_selective.md) 强化**: 之前 Hypothesis B（chunk-element 边错位）正式 falsified，新 Hypothesis C（encoder bound）supported
  - **F-formula 优先级 P0**: 之前是 EV 排序第 1，现在有**强证据**支持必须换 encoder（Qwen3-Math / Mistral-Math）。HyDE / caption injection 是次选
  - **Cost**: ~30 min wall, 0 GPU（graph rerank CPU 几分钟），$0 LLM——远低于 plan 预算 1.5h + 30min GPU
  - **副产品**: P1 bug（chunk_id 0% 一致）部分缓解；mentor B1 todo 工程价值仍在（QA / SFT 数据合成需要 deterministic alignment），只是对当前 retrieval 指标无主线增益
  - **文件**: [refine-logs/B1_PHASE2_LINENO_EXPERIMENT_20260510.md](../refine-logs/B1_PHASE2_LINENO_EXPERIMENT_20260510.md), [exp:20260510_b1_phase2_lineno](experiments/20260510_b1_phase2_lineno.md), `scripts/rebuild_chunk_element_edges_lineno.py`, `scripts/inject_lineno_into_elements.py`

- 2026-05-10T13:30:00Z: **F-formula caption injection 实验执行（job 68107, ~12 min wall, ~10 min A6000）—— HD verdict, C11 strengthened**。延续 B1 Phase 2 推论"formula 是 encoder bound"，跑了最便宜的 F-formula 变体：用同一 Qwen3-Embedding-4B encoder + 给 formula passage 注入 300 chars NL `context_before`，看 R@10 是否突破 0.56。
  - **设计**: `scripts/build_formula_caption_corpus.py` 把 mineru `context_before` 注入 1253 formula passages (1054 by id, 199 by content jaccard, 5 no-context). 平均每 passage +303 chars NL. 其他模态 corpus 不变. Encoder / queries / qrels 完全不变.
  - **结果（M4query_v1 全集）**: dense R@10 **0.6195 → 0.5825 (−3.7pp)**, graph R@10 **0.6913 → 0.6691 (−2.2pp)**, MRR 全跌. 唯一正向 R@100 +0.7pp（gold 在 deep pool 略好找，但 top-10 被噪声推走）.
  - **Smoke50 per-modality**: formula bucket 跌得最厉害 — dense 0.5600 → **0.4000 (−16pp)**, graph 0.5600 → **0.5200 (−4pp)**. Figure / table 也跌但幅度小. **HD ("NL injection 反伤")** 命中
  - **机制等同 C8**: cross-modal style injection (visual into figure, paragraph into formula) 在 text-style query retrieval 上全方向 net 负. 把 paragraph context 注入 formula 让 dense encoder 把 passage embedding 拉向"text"方向，远离 query 真正想匹配的 LaTeX 内容
  - **C11 升级**: 6 configs ≤ 0.5600 → **8 configs 0 突破 0.5600**（其中 3 个 regressed）。"Same encoder + NL augmentation strictly cannot rescue LaTeX representation"
  - **唯一未排除杠杆**: 真换 encoder（Qwen3-Math / Mistral-Math）。Caption injection / HyDE 路线（Option β/γ in C11 v1）正式废除，与 C8 一同标记 "cross-modal style injection always net negative"
  - **统一 framing 给 paper**: "Cross-modal style injection is net negative for M4query retrieval, regardless of injection direction (visual→figure, paragraph→formula)" — 一个 strong negative finding 值得在 paper 里讲
  - **文件**: [F_FORMULA_CAPTION_DECISION_20260510.md](../refine-logs/F_FORMULA_CAPTION_DECISION_20260510.md), [exp:20260510_f_formula_caption](experiments/20260510_f_formula_caption.md), `scripts/build_formula_caption_corpus.py`, `slurm_scripts/50_f_formula_caption_inject.sh`
  - **战略含义**：(i) 三轮 reranker 失败的真根因不是 modality bias 而是"graph 已摘完低垂果子"；(ii) formula 是真正瓶颈（dense/graph/qwen3 在 formula 上都卡 0.56），需 math-aware encoder（**F-formula** 推荐项）；(iii) 所有 reranker 路线（BGE/Qwen3/corpus enrich）已证伪，不再追。
  - **主计划文档**：[refine-logs/BCD_PHASED_PLAN_20260510.md](../refine-logs/BCD_PHASED_PLAN_20260510.md)。

- 2026-05-10T17:15:00Z: **F-formula Phase 3 — Qwen2.5-Math-7B routing COMPLETED (job 68281), D2 partial lift = FIRST POSITIVE FORMULA SIGNAL in 10 configs**.
  - smoke50 formula bucket R@10: 0.5600 → **0.6000** at RRF k=60 (+4pp, n=25)
  - full M4query formula bucket R@10: 0.5600 → **0.6313** at k=60 (+7.3pp, n=179, more reliable)
  - formula bucket R@100: 0.8636 → **0.9441** (+8pp recall ceiling lift)
  - Cost: overall full R@10 0.6195 → 0.5222 at k=60 (−9.7pp), because RRF symmetric fusion over-weights formula relative to figure/table.
  - **C11 partially falsified**: math-aware encoder is the first lever to break formula 0.56 ceiling. Claim needs softening from "ceiling bound" to "ceiling LIMITED with modality-routing tradeoff".
  - Phase 2 candidates: (a) query-conditional routing (only fuse Math when query has formula-style anchors); (b) two-stage cascade (dense → math rerank only inside top-100). Both should preserve overall R@10 while keeping formula gain.
  - Operational notes: job 68269 (first try, ~15:42Z) died at HF model download because CephFS quota was 4.6 GB over (304.6/300 GB). Freed 15 GB by removing unrelated SDXL from `~/.cache/huggingface/hub/` (user confirmed it was from another project but freely re-downloadable from HF). Re-submitted as 68281 which then succeeded. Subsequently also removed dead `data-process-test/Qwen3-VL-30B-A3B-Thinking/` (62 GB, last touched 2026-02-08, zero references in scripts/wiki, replaced by Claude API since 2026-02-22). Quota usage 304.6 → 232.2 GB, headroom restored to ~67 GB.

- 2026-05-10T15:30:00Z: **F-formula Phase 3 — Qwen2.5-Math-7B routing job 68269 submitted (RUNNING on gpu-a6000-1)**. C11 唯一未试杠杆：真换 math-aware encoder。Mode B (formula-only routing + RRF) isolates math-aware signal to formula bucket so figure/table regression can't mask gains.
  - Implementation: `scripts/eval_formula_qwen25math_routing.py` — Qwen2.5-Math-7B as encoder via mean-pool last hidden state + L2 norm (hidden_size=3584). Encode 1253 formula passages + 473 queries with Math-7B; cosine sim → math formula ranking; RRF fuse with Qwen3-4B baseline ranking (sweep k ∈ {10, 20, 60}).
  - Slurm: `slurm_scripts/52_f_formula_qwen25math_routing.sh`, A6000 / 2h / 24G mem (capped from requested 48G). First run downloads Qwen2.5-Math-7B (~14GB, ~10 min) before encoding.
  - Decision rules pre-set: HD if `formula_bucket_smoke50.R@10` ≤ 0.56 (C11 fully validated); D2 (0.56, 0.62] partial; D1 > 0.62 first ceiling break → paper main result.
  - User confirmed Mode B (vs A unified) explicitly to avoid math-7B-on-non-formula regression confound. Doc page: [exp:20260510_f_formula_qwen25math_routing](experiments/20260510_f_formula_qwen25math_routing.md).
  - 同 session 还更正 wiki：D1 todo 已发（之前 wiki 错记为草稿压着 19 天）；A2 文档建图.md 5/10 07:38 已重写（之前 wiki 错记为 5/2 后未动）；并对 `文档建图.md` 第十节去重命名 + 第十一节 verdict 段加 modality-selective scope + cross-modal injection 负向 finding + encoder 候选具名清单。

- 2026-05-10T14:54:00Z: **F-formula Phase 2 — LaTeX normalization (job 68131, ~4 min GPU) — HD FAIL, surface form hypothesis killed**。LaTeX symbol normalization (`\operatorname`->opt, `\leq`-><=, `\mathbb{E}`->E, `\frac{a}{b}`->(a)/(b)) has zero effect on Qwen3-Embedding-4B encoding. Formula R@10 stays at 0.5600 in both dense and graph configs (vs baseline 0.5600). Figure/table unaffected.
  - Method: `scripts/build_math_normalized_corpus.py` normalizes 1253 formula passages from raw LaTeX to readable text. Qwen3-Embedding-4B re-encodes -> dense retrieval -> graph rerank -> smoke50 slice.
  - Data: dense R@10 0.6237 (vs 0.6195, +0.4pp noise), graph R@10 0.6977 (vs 0.6913, +0.6pp noise). Formula bucket: dense 0.5600, graph 0.5600 — identical to baseline.
  - Falsified hypothesis: "LaTeX tokenization is the formula bottleneck root cause" — killed by data. Even normalized "sum over i=1 to n of X_i" is no better than raw "$$\sum_{i=1}^{n}X_i$$".
  - [claim:C11](claims/C11_formula_ceiling_is_dense_encoder_bound.md) upgraded to 10 configs: 3 strategy classes exhausted (graph topology x4, NL augmentation x3, surface form x2 + reranker family = 10). 0 configs break 0.5600 on formula bucket. Last lever: true math-aware encoder (jina-embeddings-v3 / Qwen2.5-Math hidden states).
  - Files: [F_FORMULA_MATH_NORM_DECISION_20260510.md](../refine-logs/F_FORMULA_MATH_NORM_DECISION_20260510.md), [exp:20260510_f_formula_math_norm](experiments/20260510_f_formula_math_norm.md), `scripts/build_math_normalized_corpus.py`, `slurm_scripts/51_f_formula_math_norm.sh`.
